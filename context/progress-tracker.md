# Progress Tracker - Isotope

Update this file after every meaningful implementation
change.

## Current Phase

- In progress

## Current Goal

- Configurable Multi-Step Follow-Up Sequences (`context/plans/followup-sequences-plan.md`) — **all 8 phases complete.** Feature fully implemented, all static verification passing. The vitest harness gap flagged during Phase 8 has since been fixed (see below) — real local test coverage is now possible, but still needs a live Clerk session token for authenticated-route assertions.

## Completed

- **Status-change notes for Companies/Contacts/Jobs (2026-08-04)**: user asked for a note to be attachable whenever the status of a Company, Contact, or Job changes, visible as history in that entity's detail panel. No existing polymorphic notes/activity-log table existed (`notes` is a personal freeform list, unrelated to entities; `contactHistory` is contact-only and message-shaped, not a generic note log) — built a new one from scratch, mirroring the `Notes` golden file structurally.
  - **New polymorphic table `status_change_notes`** (`apps/backend/src/db/tables.ts`): `entityType` (text, `StatusChangeEntityTypeEnum`: Company/Contact/Job), `entityId`, `fromStatus`/`toStatus` (int), `note` (nullable text), `createdBy`, `createdAt`. Indexed on `(entityType, entityId)` and `createdBy`. Migration `0024_isotope.sql` generated but **not yet applied to remote staging D1** — `wrangler` in this session hit a Cloudflare auth error (`code: 7403`, account not authorized); user needs to run `pnpm db:migrate` from `apps/backend` after re-authenticating (`wrangler login`).
  - New `packages/schemas/src/status-change-notes/` (5 files, standard Common/ApiRequest/ApiResponse/DALRequest/index shape) + `StatusChangeNotesDAL/Repo/Routes.ts` (mirrors `NotesDAL.ts` method-for-method) — `POST /status-change-notes` (single), `POST /status-change-notes/bulk` (one note applied to every id in a batch, used by bulk status-update flows), `GET /status-change-notes?entityType=&entityId=`. Mounted in `workers/api/index.ts`.
  - **Frontend**: new shared (not entity-scoped) `apps/web/src/routes/_authenticated/-StatusChangePopover.tsx` (status `<select>` + optional note `<textarea>` in a small popover, replaces every prior direct-mutate status control) and `-StatusChangeHistory.tsx` (chronological from→to + note list) + `-status-change-notes-data.ts` (`useCreateStatusChangeNote`, `useBulkCreateStatusChangeNotes`, `StatusChangeNotesQueries`). Wired into **6 surfaces**: Companies panel (`companies/-DesktopPanel.tsx`) + full page (`companies/$companyId/index.tsx`) — Accept/Reject buttons and the status badge now open the popover pre-seeded with the target status instead of mutating directly; Contacts panel (`contacts/-ContactDetailContent.tsx` + `-AboutTab.tsx`, shared by both the slide-out panel and the full page route) — status badge now opens the popover, history renders in the About tab; Jobs panel (`jobs/-JobPanelContent.tsx` + `-JobPanelDetails.tsx`) and full page (`jobs/-JobDetailBody.tsx`) — same treatment.
  - **Bulk status-update flows** (Contacts desktop/mobile, Jobs desktop/mobile — 4 UI surfaces) extended per user decision: one shared optional note applies to the whole batch. Desktop tables (`contacts/-DesktopContactsTable.tsx`, `jobs/-JobsTable.tsx`) use a Popover on the "Apply" button; mobile lists (`contacts/-MobileContactsList.tsx`, `jobs/-MobileJobsList.tsx`) use an inline `<textarea>` in the existing fixed-bottom bulk bar instead (a Popover anchored inside a bottom-fixed sheet was judged too awkward on small viewports). `index.tsx` for both entities now fires `useBulkCreateStatusChangeNotes` after the existing bulk-update mutation succeeds.
  - **Removed the redundant inline status `<select>`** from all three entities' edit modals (`-AddOrEditCompanyModal.tsx`, `-AddOrEditContactModal.tsx`, `-AddOrEditJobModal.tsx`) per explicit user decision — status changes now flow exclusively through the popover, avoiding a second silent no-note path. Each modal's `STATUS_OPTIONS` array was exported (previously module-private) for reuse by the popover/history components in the same feature folder.
  - Verification: `tsc --noEmit` clean in all three packages (`packages/schemas`, `apps/web` zero errors; `apps/backend` at the exact pre-existing baseline — `EnvConfig.ts`, `dbClient.ts`, `AiProvider.ts`, `ContactsRepo.ts:325`, `JobsRepo.ts:146`, `NotesRepo.ts`, `EmailInboundRoutes.ts`, `notes.test.ts`, `setup.ts`, `JobDiscoveryWorkflow.ts` — none touched). `eslint` clean on every new/touched file across all three packages (zero new errors or warnings; pre-existing findings in untouched files like `useAddShortcut.ts` and `JobSearchFrameworkForm.tsx` confirmed via `git status` to predate this change). Not exercised in a live browser session — no Clerk credentials this session (same recurring limitation as every prior unit in this tracker) — and the migration is unapplied, so the feature cannot be smoke-tested end-to-end until both are resolved.
  - **Outstanding**: (1) run `pnpm db:migrate` from `apps/backend` once Cloudflare auth is restored; (2) manually verify in a real browser: status-change popover opens/saves correctly from every one of the 6 single-entity surfaces and the note appears in history; bulk popover/textarea note applies to every row in a batch; the fixed-bottom mobile bulk bars don't visually clip the new inline textarea on small screens.

- **Auto-complete follow-up task on outbound message log (2026-07-29)**: user reported having to manually check off the Tasks-tab checkbox after logging a follow-up message. Research surfaced a real conflict: `syncFollowUpForContact` reused a single row per contact+channel forever (UPDATE-not-INSERT, `followup-sequences-plan.md` decision #3) — a "complete current step, keep advancing" model was confirmed with the user, which requires the prior active row to actually flip to `Completed` and a new row to be inserted for the next step, overturning decision #3 (see addendum appended to that plan doc, and the corrected `tasks` bullet in `context/architecture.md`).
  - `TasksDAL.syncFollowUpForContact` now takes `completePriorStep: boolean` (new field on `SyncFollowUpDALRequest`): `true` marks the existing Pending/Paused row `Completed`+`completedAt` and always inserts a fresh Pending row for the next step; `false` preserves the old in-place UPDATE (due date/step move only, no completion). Threaded through `TasksRepo.syncFollowUpForContact` → `ContactsRepo.resyncFollowUp` → its 3 call sites: `logContactHistory` (outbound message → `true`), `updateContactHistory` and `hardDeleteContactHistory` (edit/undo recompute → `false`, since no new touch occurred).
  - New `TasksDAL.updateActiveFollowUpStatus`/`TasksRepo.updateActiveFollowUpStatus` — when an outbound message finishes the sequence (`stepNumber > offsetDays.length`) with `completePriorStep=true`, the dangling row is now marked `Completed` (visible in Past tasks) instead of deleted; the edit/undo recompute path still deletes it in that case (no real completion happened).
  - New guard in `TasksDAL.updateTaskStatus`: blocks un-completing a task row if a newer row already exists for the same contact+channel — prevents the manual Tasks-tab checkbox from resurrecting a superseded historical step into a second simultaneously-active row (every other follow-up DAL method assumes at most one active row per contact+channel). Surfaces as a toast via the existing `ApiError`/`onError` path, no frontend change needed.
  - `IDX_tasks_contact_id_channel` was already a plain (non-unique) index — no schema migration required; a contact+channel now naturally accumulates one `Completed` row per finished step plus at most one active row.
  - Audited every task query (calendar, day view, past, search, sweep) for single-row-per-channel assumptions before writing code — all were already correctly scoped by `status IN (...)` filters, so only the two spots above needed changes. Flagged one pre-existing, unfixed gap (not introduced by this change, just more likely to occur now): `getPastTasks` filters `dueAt < today`, so a task completed before its originally-scheduled due date briefly disappears from both the day view and Past tasks until that date passes.
  - Verification: `tsc --noEmit` clean in `packages/schemas`; `apps/backend` back to the exact pre-existing baseline error set (`EnvConfig.ts`, `dbClient.ts`, `AiProvider.ts`, `JobsRepo.ts`, `NotesRepo.ts`, `EmailInboundRoutes.ts`, `notes.test.ts`, `setup.ts`, `JobDiscoveryWorkflow.ts`, `ContactsRepo.ts:325` — none touched by this change). Not yet exercised against a live authenticated session (no Clerk credentials this session, same recurring limitation as prior units in this tracker) — recommend manually testing the full lifecycle (log outbound → confirm prior task shows Completed in Past tasks + new Pending task appears; edit a message's date → confirm due date moves without a spurious completion; finish a sequence → confirm the last task shows Completed, not deleted) before considering this fully verified.

- **Multi-step log-message templates (per follow-up step + Day-0 role-type variants) + `[Name]`/`[Company]` substitution engine (2026-07-26)**: Started as a single flat default-body template (see superseded design below), then reshaped mid-session after the user clarified the real requirement — an email-campaign-style sequence (Day 0 / Day 3 / Day 8 / Day 14 …) with the initial touch (step 0) having up to 3 role-type-based variants (e.g. "Backend" vs "Full Stack"), auto-selected from the contact's company's job listings. Foundation for future bulk-log flows — `renderTemplate` (in `packages/schemas`) and the server-side resolution logic are both reusable as-is.
  - **New concept: Role Types** — up to 3 user-defined labels (e.g. "Backend", "Full Stack", "AI Engineering") with one marked default. Non-versioned single row per user, `role_types` table (`id`, `createdBy` UNQ-indexed, `labels` JSON array, `defaultLabel` nullable, `isCustomized`, timestamps). Backs two things: (1) a new `jobs.roleType` nullable text column, settable via a pill-select on `-AddOrEditJobModal.tsx` (prefilled with the user's default on **add** only, via an async-load `useEffect` guarded by a ref — same pattern as the existing "prefill latest company" effect); (2) the Day-0 template variant slots.
  - **`log_templates` reshaped from single-row-per-user to one row per `(user, step, variantLabel)`**: `step` (0 = initial outbound, 1..N = follow-up N, matching `followup_settings.stepOffsetDays[N-1]` 1:1 so the two stay in sync by construction), `variantLabel` (nullable text — only ever populated at step 0, one row per configured role-type label, **plus** one `variantLabel = NULL` row representing the "default" slot; steps ≥ 1 always have exactly one `variantLabel = NULL` row, no variants). No DB-level uniqueness on `(created_by, step, variant_label)` is reliable when `variant_label IS NULL` — SQLite treats NULLs as distinct in a UNIQUE index — so `LogTemplateDAL.saveTemplate` does an explicit find-then-update-or-insert rather than `ON CONFLICT`, documented inline. **Migration history is two-step**: `0017_isotope.sql` (`DROP TABLE log_templates`, undoing the prior flat-body table — it only ever held empty seed rows, safe to drop) then `0018_isotope.sql` (`CREATE role_types`, `CREATE log_templates` in the new shape, `ALTER TABLE jobs ADD role_type`) — drizzle-kit's interactive rename-detection prompt (no TTY available in this session) forced generating the drop and the additions as two separate passes rather than one combined diff; both applied cleanly to remote staging D1.
  - `packages/schemas/src/role-types/` (new, mirrors `contact-role-pills` structurally) — `RoleTypesCommon.ts`/`ApiRequest`/`ApiResponse`/`DALRequest`/`index.ts`. `packages/schemas/src/log-template/` reshaped — `LogTemplateEntry` (`step`, `variantLabel`, `body`) replaces the old flat `LogTemplateInput`; `renderTemplate(template, { name, company })` unchanged (`[Name]`→name, `[Company]`→company or literal `"[Company]"` if missing). New `ResolveLogTemplateApiResponse` (`step`, `variantLabel`, `renderedBody`, `isAmbiguousMatch`) — the shape returned by the new resolve endpoint, pre-rendered server-side so the frontend never needs the raw template body or `renderTemplate` itself for the log dialog. `jobs.roleType` (nullable string) added to `ZJobBase` in `JobsCommon.ts` — `CreateJobApiRequest`/`UpdateJobApiRequest`/DAL request types all pick it up automatically since they derive from `ZJobBase`/`Job` generically.
  - Backend: `RoleTypesDAL/Repo/Routes` (`GET`/`POST /role-types`) — near-identical to the old flat `LogTemplateDAL` from the superseded design (single-row-per-user, `ON CONFLICT` upsert is valid here since `created_by` alone is the unique key, no NULL-column complication). `LogTemplateDAL/Repo/Routes` rewritten — `getTemplates` (list all rows for user, ordered by step), `findTemplate` (exact step+variant lookup, `isNull()` when variant is null), `saveTemplate` (find-then-write, see above). `JobsDAL.createJob`/`updateJob`/all three `select()` blocks (`getJobDetails`, `getJobs`, `getJobsByCompany`) now read/write `roleType`. **New `ContactsRepo.resolveLogTemplate`** (`GET /contacts/:id/log-template`) — the actual resolution logic: derives `step` via the same `getSentMessageCount` the existing `resyncFollowUp` uses (0 = no sent messages, so mirrors the scheduler's own step math rather than trusting the follow-up task's `stepNumber`, which can be absent once a sequence completes); at step 0 with a `companyId`, calls `JobsRepo.getJobsByCompany` and collects distinct non-null `roleType` values — exactly one match uses that variant, zero or multiple falls back to `RoleTypesRepo`'s `defaultLabel` (multiple also sets `isAmbiguousMatch: true`); calls `LogTemplateRepo.findTemplate` for the resolved step/variant and renders it via `Schemas.renderTemplate` before returning. `UsersRepo.syncClerkUser` seeds a default (empty) `role_types` row per new user; `log_templates` rows are created lazily as the user writes each step/variant in Settings — no seed needed.
  - Frontend Settings: two new tabs replacing the old single "Log Template" tab — **Role Types** (`-RoleTypesForm.tsx`, pill-repeater + star-icon default picker, mirrors `-ContactRolePillsForm.tsx`'s repeater UI) and **Log Templates** (`-LogTemplatesForm.tsx`, renders `stepOffsetDays.length + 1` step sections; step 0 shows one textarea per configured role-type label plus a "Default" slot, steps ≥ 1 show a single textarea; each slot saves independently with its own "Saving…/Saved" state, not one big form submit). `LogTemplateQueries` changed from a single `.latest()` query to `.all()` (returns the full list); new `RoleTypesQueries`/`useSaveRoleTypes` added to `settings/-data.ts`.
  - Frontend log dialog: `AddOrEditContactHistoryForm.tsx`'s template integration reworked to call the new `GET /contacts/:id/log-template` resolve endpoint (`ContactsQueries.logTemplate`, new query in `contacts/-data.ts`) instead of fetching the raw template and rendering client-side — the `contact: Schemas.Contact` prop from the prior design was dropped entirely (no longer needed now that substitution happens server-side; `HistoryTab.tsx`'s call site updated to match). Seeds the body from `resolved.renderedBody` once, only if untouched, same guard pattern as before. New ambiguous-match banner (muted `--warning-text`, matching the existing Job Search tab's notice styling) shown when `resolved.isAmbiguousMatch` is true: _"This company has multiple job types listed, so we used your default template instead of guessing."_ The "no template configured" discoverability hint (linking to `/settings`) is unchanged in spirit, now driven by `!resolved?.renderedBody`.
  - **Confirmed gaps in scope, deliberately not built**: no bulk-log/bulk-compose UI exists yet anywhere in the codebase — this feature only wires the _single_-log dialog; a future bulk surface reads from the same `resolveMessageTemplate`/`renderTemplate`. Variants are step-0-only per explicit user decision (steps ≥ 1 in the campaign example had no variants either). Role-type matching is a simple exact-string match on `jobs.roleType`, not fuzzy/keyword matching against `jobs.title` — deliberate, since free-text title matching was flagged as unreliable and the user chose explicit tagging instead.
  - **Full rename: "log template" → "message template", everywhere (2026-07-26, same day)**: user asked for the concept renamed end-to-end — UI copy, file names, variable/type names, route paths, table name — after noticing `LogTemplateRoutes.ts`. Every identifier above (`LogTemplate*` → `MessageTemplate*`, `logTemplate(s)` → `messageTemplate(s)`) reflects the **final** post-rename state; treat this note as the authoritative naming, not the paragraphs above. Concretely: `packages/schemas/src/log-template/` → `message-template/` (all 4 files + barrel renamed); `LogAction.GetLogTemplates/SaveLogTemplate/ResolveLogTemplate` → `GetMessageTemplates/SaveMessageTemplate/ResolveMessageTemplate` in `log.ts` (the unrelated `LogCategory`/`LogAction` logging-infra names themselves were correctly left untouched — only the 3 feature-specific enum values were in scope); backend `LogTemplateDAL/Repo/Routes.ts` → `MessageTemplateDAL/Repo/Routes.ts`, mounted at `/message-template` (was `/log-template`); `ContactsRepo.resolveLogTemplate` → `resolveMessageTemplate`, route `GET /contacts/:id/log-template` → `/contacts/:id/message-template`; `tables.ts`'s `logTemplates` table var/DB table → `messageTemplates`/`message_templates`, indexes renamed to match (`IDX_message_templates_user_step`, `UNQ_message_templates_user_step_variant`). Frontend: `-LogTemplatesForm.tsx` → `-MessageTemplatesForm.tsx`; `LogTemplateQueries`/`useSaveLogTemplate` → `MessageTemplateQueries`/`useSaveMessageTemplate` in `settings/-data.ts`; Settings tab key/label `"log-templates"`/"Log Templates" → `"message-templates"`/"Message Templates"; `ContactsQueries.keys.logTemplate`/`.logTemplate()` → `.messageTemplate`/`.messageTemplate()` in `contacts/-data.ts`, consumed by `-AddOrEditContactHistoryForm.tsx`.
    - **DB migration is a true rename, not drop+recreate** — `0019_isotope.sql`: `ALTER TABLE log_templates RENAME TO message_templates` + drop/recreate the two indexes under their new names. Chosen specifically to preserve any templates a user might have already saved via the Settings UI during this session (unlike the earlier drop+recreate migrations 0016-0018, which were safe to drop only because that table held nothing but empty seed rows at the time). Historical migration files `0016`-`0018` were **not** edited — migration files are an immutable append-only record; only `0019` (new) references the old `log_templates` name, correctly, since it's the rename's _source_.
    - drizzle-kit's interactive rename-detection prompt has no TTY in this session (same limitation hit during the original reshape) — worked around by hand-writing `0019_isotope.sql` directly and hand-patching drizzle's own state (`meta/0019_snapshot.json` cloned from `0018`'s with the table/index keys renamed, `meta/_journal.json` appended with the new entry) so drizzle-kit's own bookkeeping stays internally consistent. Verified by re-running `pnpm db:generate` immediately after — it reported "No schema changes, nothing to migrate 😴", confirming the hand-written snapshot exactly matches `tables.ts`.
    - Verification: `tsc --noEmit` clean across all three packages (zero errors, exact pre-existing backend baseline). `eslint` clean on every renamed/touched file (only pre-existing warnings). `wrangler dev --env staging` smoke test: `GET`/`POST /message-template` and `GET /contacts/1/message-template` all correctly 401; the **old** `GET /log-template` now correctly 404s, confirming the rename fully replaced rather than duplicated the mount. Migration `0019` applied to remote staging D1 (6 commands executed, ✅).
  - Verification: `tsc --noEmit` clean in `packages/schemas` and `apps/web` (zero errors, across both the initial flat design and the final reshape). `apps/backend` at the exact pre-existing baseline error set throughout (`EnvConfig.ts`, `dbClient.ts`, `AiProvider.ts`, `JobsRepo.ts`, `NotesRepo.ts`, `EmailInboundRoutes.ts`, `notes.test.ts`, `setup.ts`, `JobDiscoveryWorkflow.ts` — none touched). `eslint` clean on every touched/created file after fixing 3 real findings mid-build (an unused `contact` prop left over from the superseded design, a `Schemas` import needing `import type`, two genuinely-unused `StepSlot` props); remaining warnings are pre-existing (`no-array-index-key` on skeleton loaders, one untouched `new Date()` purity warning). Ran `wrangler dev --env staging` and curled `GET`/`POST /role-types`, `GET`/`POST /log-template`, and `GET /contacts/1/log-template` without an auth token — all five correctly 401 (not 404/500), confirming every new route is mounted and `checkAuth`-gated; dev log had zero unexpected errors. Both migrations (`0017`, `0018`) applied to remote staging D1 cleanly. Could not exercise the authenticated fetch/save/pre-fill/variant-resolution round-trip in a real browser session — no Clerk sign-in credentials available in this session (same limitation noted across every prior feature in this tracker).

- **Vitest/Workers test harness fix (2026-07-21)**: `apps/backend`'s `npx vitest run` was completely broken pre-existing (discovered during the Follow-Up Sequences feature's Phase 8 verification) — fixed as its own small unit.
  - **Root cause 1**: `vitest.config.mts` pointed `wrangler: { configPath: "./wrangler.jsonc" }` at a path that stopped existing after the Spec 06 API/processor worker split — the real config lives at `workers/api/wrangler.jsonc`. Fixed the path.
  - **Root cause 2**: with that path fixed, the pool then failed on missing env vars (`ALLOWED_CORS_ORIGIN`, etc.) because the wrangler config only defines `vars`/`d1_databases`/`ai`/`workflows`/`queues` bindings inside the named `staging`/`production` environments, not at the top level — and both of those envs mark `d1_databases` with `remote: true`, meaning selecting either as `wrangler.environment` makes the test suite establish a real network connection to the actual shared staging D1 (confirmed via a live "Establishing remote connection..." log line and a subsequent unexplained 500 — not something a unit test should ever do). Fixed by **not** selecting a named environment at all, and instead supplying every binding the test needs as local-only `miniflare.bindings`/`d1Databases` overrides in `vitest.config.mts` (real dev Clerk keys already in `.dev.vars`, since Clerk's own key-format parser rejects a syntactically fake key and crashes to 500 before even evaluating the request — a genuinely fake string isn't valid base64-decodable Clerk key shape).
  - **Root cause 3**: `notes.test.ts` imported `worker from "../index"`, which hasn't existed since the same worker-entry-point move (now `workers/api/index.ts`) — fixed the import path. Also fixed its two `vi.mock()` targets (`@/providers/clerk` → `@/providers/ClerkProvider`, `@/providers/logger` → `@/providers/AppLogger`) to match the real file names.
  - **Root cause 4 (structural, not just a path bug)**: even with all paths fixed, `vi.mock("@/providers/ClerkProvider", ...)` never actually intercepts anything under `@cloudflare/vitest-pool-workers` — confirmed via Context7 docs on the package's own plugin source: the pool injects the worker's entry-point as a side-effect-only import into the internal `cloudflare:test` module ahead of any `vi.mock()` registration in the test file, so the entry-point's transitive imports (including `ClerkProvider`) are never routed through Vitest's mock-aware module loader. Verified empirically too: with a syntactically valid dummy key, the "authenticated" tests came back 401 instead of the mocked 200/201 — proof the mock was silently inert, not a fluke. Rather than leave a fake-passing mock in place, `notes.test.ts` was rescoped to only assert what's honestly testable offline (the unauthenticated 401 path, which needs no mock — Clerk's real SDK genuinely rejects a missing/invalid token) — with a `DEV_NOTE` explaining why authenticated-route coverage isn't possible without either a real Clerk test-mode session token or the package's documented "auxiliary worker" test pattern.
  - New `src/tests/setup.ts` — applies all 14 existing D1 migrations (`readD1Migrations`/`applyD1Migrations`, the package's own documented fixture pattern) to the local Miniflare-simulated `DB` binding before tests run, via a `TEST_MIGRATIONS` binding computed once in `vitest.config.mts` and a `test.setupFiles` entry.
  - Verification: `npx vitest run` passes 2/2 tests, consistently, across 3 consecutive runs (~1.5s each, fully local, zero network calls). `tsc --noEmit`/`eslint` on `notes.test.ts`/`setup.ts`/`vitest.config.mts` clean (the pre-existing `cloudflare:test` module-resolution errors under plain `tsc --noEmit` are a separate, known gap — the root `tsconfig.json`'s `types` array was never wired to the pool's ambient types; vitest resolves the module correctly at actual test-run time via its own Vite-based toolchain, which is what matters for `vitest run` to work).

- **Follow-Up Sequences — Phase 1-8, feature complete (2026-07-21)**: schema types + DB migration + isolated Follow-up Settings backend + Tasks DAL/Repo primitives + ContactsRepo wiring + Follow-up Settings frontend + Tasks UI updates + final end-to-end verification for configurable multi-step follow-up cadence (see `context/plans/followup-sequences-plan.md`, all phases 1-8 done).
  - **Phase 8 (end-to-end re-verification across the fully integrated feature)**: re-ran typecheck + lint across all three packages as a single consolidated pass — `packages/schemas` (0 errors), `apps/backend` (16 pre-existing baseline error lines / 14 distinct locations, identical to every prior phase checkpoint, in files this feature never touched: `EnvConfig.ts`, `dbClient.ts`, `AiProvider.ts`, `JobsRepo.ts`, `NotesRepo.ts`, `EmailInboundRoutes.ts`, `notes.test.ts`, `JobDiscoveryWorkflow.ts`), `apps/web` (0 errors). `eslint` across all three: only pre-existing errors/warnings in untouched files, plus the two already-accepted `no-array-index-key` warnings in Phase 6's new files (matching the golden `PrioritisedSkillsRepeater` convention).
  - **Discovered the repo's `apps/backend` vitest/`cloudflare:test` harness is broken independent of this feature** — `vitest.config.mts` still points `wrangler: { configPath: "./wrangler.jsonc" }` at the pre-Spec-06 root path; the actual config lives at `workers/api/wrangler.jsonc` since the API/processor worker split. `npx vitest run` fails at pool-startup (`ENOENT wrangler.jsonc`) before any test executes — this is why `notes.test.ts`'s `cloudflare:test`/`../index` import errors have appeared as "pre-existing baseline" in every phase's `tsc` output; there was never a way to get a real authenticated/DB-backed test run in this session via the existing harness. Not fixed as part of this feature (out of scope, pre-dates all 8 phases) — flagged as a real gap for a future unit.
  - **Static/code-level correctness re-checks specific to Phase 8** (in lieu of a live authenticated D1 exercise, which needs Clerk credentials this session never had): confirmed zero remaining references anywhere in the repo to the removed `Constants.TASK_FOLLOWUP_INTERVAL_DAYS` and to the renamed `deletePendingFollowUp`/`DeletePendingFollowUpTask`/`DeletePendingFollowUpDALRequest` (full grep across `apps/backend/src`, `apps/web/src`, `packages/schemas/src`). Confirmed `resumeFollowUpForContact` exists only as the two DAL/Repo methods with zero call sites elsewhere — correctly unwired per decision #10. Re-confirmed `TasksDAL.sweepOverdueTasks`'s WHERE clause is byte-for-byte unchanged from before this feature (`eq(tasks.status, Pending)`, an explicit positive filter) — so a Paused task with a past-due `dueAt` is never swept to Missed by the nightly cron, satisfying plan §7 without any code change (confirmed by reading the query, not by running the cron live). Re-confirmed `CONTACT_HISTORY_SENT_TYPES` (`["email_sent", "linkedin_sent"]`) is the single shared filter used identically by both `getLastSentHistory` and `getSentMessageCount`, so `resyncFollowUp`'s `sentCount` and `lastSentAt` are always computed over the same row set — no scope-drift bug between the two reads.
  - **Overall session limitation, stated once for the whole feature**: every phase's "done when" checklist in the plan calls for a live, authenticated, real-D1 exercise of the full lifecycle (send → follow-up appears → reply → pause → send → advance → sequence-complete → delete → Dead → deadAt+delete). None of that was executable in this session — no Clerk sign-in credentials, and the repo's own Workers-test harness turned out to be broken pre-existing. What **was** done instead, across all 8 phases: full static verification (typecheck/lint at every checkpoint, zero new errors introduced at any point), hand-traced logic verification against the plan's own worked examples (Phase 5, with `stepOffsetDays=[3,5,7]`), and runtime smoke tests via `wrangler dev`/`vite dev` confirming every new/changed route and page SSRs and mounts correctly (401-not-404 on all follow-up-settings/tasks/contacts endpoints, successful SSR route-match resolution on `/settings` and `/tasks`). A real authenticated run-through remains the one outstanding verification step, blocked purely on session credentials/infra, not on anything found wrong in the implementation.
  - **Phase 7 (frontend Tasks UI updates)**: confirmed `-TaskSection.tsx` has no hardcoded `[Pending, Completed, Missed]` status enumeration — it's fully status-agnostic (operates on `tasks.length`/loading/error only), so Paused rows pass through with zero risk of being silently excluded/misplaced; no edit needed there. `-TaskRow.tsx` (MODIFY) — added a step badge (`task.stepNumber != null` → renders `"Follow-up N"`, no "of total" denominator, matching decision #8's avoidance of coupling the Tasks page to Follow-up Settings) and a `"Paused"` badge (`task.status === TaskStatusIntEnum.Paused`) using the same muted `<span>` visual pattern as the existing `Overdue {n}d` badge; `isOverdue` now also excludes Paused rows (`!isCompleted && !isPaused`) since a Paused task's `dueAt` is stale/pending-resync, not meaningfully "overdue". `Checkbox` has no status-based `disabled` guard beyond `isUpdating` — confirmed already satisfies decision #8 (Paused tasks stay manually completable) with zero change needed. `-data.ts`, `-WeekStrip.tsx`, `index.tsx` confirmed pass-through only (generic over `Schemas.TaskWithMeta`, `toggleTask` already toggles between `Completed`/`Pending` from any status, `WeekStrip`'s calendar dots derive only from `hasPending`/`hasMissed`/`hasCompleted` which Phase 7 didn't touch) — no edits made to any of the three.
  - Verification: `tsc --noEmit` clean in `apps/web` (zero errors). `eslint` clean on `-TaskRow.tsx`. Ran `vite dev` locally and curled `/tasks` — all three route matches (`__root__`, `_authenticated`, `_authenticated/tasks/tasks`) resolved `s:"success"` server-side, confirming the new `stepNumber`/Paused-badge logic in `-TaskRow.tsx` compiles and SSRs without crashing. Could not visually confirm the badge/checkbox rendering against a real seeded task in an authenticated browser session — no Clerk sign-in credentials available in this session (same limitation as every prior phase).
  - **Phase 6 (frontend Follow-up Settings UI, independently testable against Phase 3's API — no dependency on Phase 5's ContactsRepo)**: confirmed no `-data.ts` existed yet in `apps/web/src/routes/_authenticated/settings/` (the `AccountTab`'s inbound-address query was inline) — created one. Confirmed `TagInput` is **not** a shared reusable component — it's private/co-located inside `JobSearchFrameworkForm.tsx` and is `string[]`-only, so per the plan a new minimal repeater was written rather than reused. `-data.ts` (NEW) — `FollowUpSettingsQueries.latest(getToken)` (`GET /followup-settings`), `useSaveFollowUpSettings()` mutation (`POST /followup-settings`, invalidates the `latest` query key on success, `toast.error` on failure — mirrors `useSaveFramework` exactly). `-FollowUpSettingsForm.tsx` (NEW) — `StepOffsetsRepeater` (numeric day-offset rows, "Follow-up N" label, add/remove controls, 12-step ceiling with an inline cap message, empty-state copy explaining `[]` disables follow-ups entirely), wrapped in a TanStack Form validated against `Schemas.ZFollowUpSettingsInput`. `index.tsx` (MODIFY) — new `"followups"` tab value + button + `FollowUpSettingsTab` component (loading skeleton, error state, wires the form to the query/mutation, success/error toasts per CLAUDE.md's mandatory mutation error handling).
  - Verification: `tsc --noEmit` clean in `apps/web` (zero errors). `eslint` clean on all 3 touched/created files (only pre-existing-pattern `no-array-index-key` warnings, matching the golden `PrioritisedSkillsRepeater`'s own `key={i}` convention — not new violations). Ran `vite dev` locally and curled `/settings` — response body shows all three route matches (`__root__`, `_authenticated`, `_authenticated/settings/`) resolved `s:"success"` server-side, confirming the new imports (`-data.ts`, `-FollowUpSettingsForm.tsx`) load and SSR-render without crashing. Could not click through the actual tab/form in a real browser session or exercise the authenticated save/reload round-trip — no Clerk sign-in credentials available in this session (same limitation as every prior phase).
  - **Phase 5 (`ContactsRepo.ts` rewrite — the risky logic core, done per plan §3/§5/§8a)**: `resyncFollowUp` fully rewritten — no longer a hardcoded 7-day single-step timer. Now: reads `getLastSentHistory`; if no sent messages remain, deletes any Pending/Paused follow-up task and clears `nextTouchDueAt`; otherwise resolves the user's `FollowUpSettingsRepo.getSettingsDetails().stepOffsetDays` (falls back to `Constants.FOLLOWUP_SETTINGS_DEFAULTS` if none saved yet), reads `getSentMessageCount` (= latest Touch N), derives `stepNumber = sentCount` (the next follow-up to schedule), deletes the task if `stepNumber > stepOffsetDays.length` (sequence complete), else computes `dueAt = lastSentAt + offsetDays[stepNumber-1]` and calls `syncFollowUpForContact` with the real `stepNumber`. `logContactHistory` now branches on `direction`: inbound (`Contact`) calls `TasksRepo.pauseFollowUpForContact` and returns early (does not touch `nextTouchDueAt`, matching plan §5 exactly — no resync on pause); outbound (`Me`) calls `resyncFollowUp` unconditionally, whose Phase-4-widened `syncFollowUpForContact` UPDATE branch (matches Pending OR Paused, resets `status→Pending`/`pausedAt→null`) naturally supersedes any Paused row — this is "Advance", not "Resume". `updateContact` rewritten per §8a: computes `deadAt` in the Repo (never trusts `params.contact.deadAt` as-is, since `ContactsDAL.updateContact`'s `.set()` writes it unconditionally with no fallback) — auto-stamps `Utility.getCurrentISOTimestamp()` the moment status transitions into Dead, preserves the original stamp on a Dead→Dead re-save (extra `getContactDetails` read to detect "already Dead" vs "transitioning into Dead"), and deletes the contact's active Pending/Paused follow-up task via `TasksRepo.deleteFollowUpTasks` on that same transition. New `FollowUpSettingsRepo` import. `Constants.TASK_FOLLOWUP_INTERVAL_DAYS` removed (dead — its only call site was this rewrite; confirmed zero other references).
  - Verification (full lifecycle traced through the code, per the plan's own §3/§5 worked examples, with `stepOffsetDays=[3,5,7]`): Touch 1 sent → `stepNumber=1`, task inserted `Pending`/`dueAt=touch1+3d`. Inbound reply → task flips to `Paused`, `pausedAt` stamped, `nextTouchDueAt` untouched (correct per plan). Touch 2 sent → `sentCount=2`→`stepNumber=2`, `2>3`? no → `syncFollowUpForContact`'s widened lookup finds the Paused row → UPDATE to `Pending`/`stepNumber=2`/`dueAt=touch2+5d`/`pausedAt=null` (Advance correctly supersedes Paused). Repeating past `stepOffsetDays.length` → `deleteFollowUpTasks` fires, `nextTouchDueAt=null`. Contact set to Dead → `deadAt` auto-stamped, any remaining task deleted. `tsc --noEmit` in `apps/backend` back to the exact 14-error pre-existing baseline (zero new errors). `eslint` clean on `ContactsRepo.ts`/`Constants.ts`. Ran `wrangler dev` locally — `/followup-settings`, `/tasks/past`, `PATCH /contacts/:id`, `POST /contacts/:id/history` all correctly 401 (not 404/500), confirming the new `FollowUpSettingsRepo` import resolves cleanly at runtime with no wiring break. Could not exercise the authenticated round-trip against real D1 data — no Clerk sign-in credentials available in this session (same limitation as every prior phase).
  - **Phase 4 (Tasks DAL/Repo primitives — additive; old `resyncFollowUp` behavior preserved)**: `ContactsDAL.ts` — added `getSentMessageCount` (counts `_sent`-type `contactHistory` rows via `CONTACT_HISTORY_SENT_TYPES`, next to `getLastSentHistory`). `TasksDAL.ts` — `taskSelection` now includes `stepNumber`/`pausedAt`; `syncFollowUpForContact` threads `stepNumber` through both branches, and its existing-row lookup was widened from `Pending`-only to `inArray([Pending, Paused])` with the UPDATE branch now also resetting `status → Pending` + `pausedAt → null` — required so a future "Advance" call (Phase 5) can unconditionally supersede a Paused row per plan §5, not just a Pending one (this is a no-op today since nothing yet sets status to Paused). Added `pauseFollowUpForContact` (Pending → Paused, stamps `pausedAt`, idempotent no-op otherwise) and `resumeFollowUpForContact` (shifts `dueAt` forward by the paused duration; implemented per plan §5 but intentionally not wired into any automatic trigger yet). Renamed `deletePendingFollowUp` → `deleteFollowUpTasks`, widening its WHERE to `inArray([Pending, Paused])`. `TasksRepo.ts` — mirrored all of the above as thin wrappers (`syncFollowUpForContact` now requires `stepNumber`; new `pauseFollowUpForContact`/`resumeFollowUpForContact`/`deleteFollowUpTasks`).
  - **Mechanical call-site rename** (the one place Phase 4 touched `ContactsRepo.ts`, rename-only per the plan's own instruction — no logic rewrite, deferred to Phase 5): `resyncFollowUp`'s call to `tasksRepo.deletePendingFollowUp` → `tasksRepo.deleteFollowUpTasks`; its call to `syncFollowUpForContact` now passes `stepNumber: 1` (today's system is single-step, so this is always step 1 — the real `getSentMessageCount`-derived computation lands in Phase 5). Also removed the now-dead `@deprecated` `DeletePendingFollowUpDALRequest` type and `LogAction.DeletePendingFollowUpTask` value from `packages/schemas` (both were transitional Phase-1 placeholders kept only until this rename landed).
  - Verification: `tsc --noEmit` clean in `packages/schemas` (fixed one new `eslint` `consistent-type-imports` violation in `FollowUpSettingsApiRequest.ts` — `z` was only used in type position). `apps/backend` `tsc --noEmit` is back to the exact 14-error pre-existing baseline (the Phase 2/3 checkpoint's one _expected_ extra error, `TasksRepo.ts:95` missing `stepNumber`, is now resolved by this phase's own change). `eslint` clean on all touched backend files; `packages/schemas` has only its 2 pre-existing, unrelated `no-empty-object-type` errors (`ContactsApiResponse.ts`, `JobsApiResponse.ts`).
  - **Phase 3 (Follow-up Settings backend, isolated — nothing existing depends on it yet)**: `FollowUpSettingsDAL.ts` (NEW, mirrors `FrameworksDAL.ts` method-for-method) — `getSettingsDetails` (latest by `createdBy` ordered `version DESC LIMIT 1`), `saveSettings` (auto-increments version, JSON-stringifies `stepOffsetDays`, delegates to `createSettings`), `createDefaultIfAbsent` (idempotent seed from `Constants.FOLLOWUP_SETTINGS_DEFAULTS`), `createSettings` (raw insert), private `deserialise`/`parseJsonArray` helpers. `FollowUpSettingsRepo.ts` (NEW, mirrors `FrameworksRepo.ts`) — thin `getSettingsDetails`/`saveSettings` wrappers with `AppLogger.info`, pure delegation to DAL. `FollowUpSettingsRoutes.ts` (NEW, mirrors `FrameworksRoutes.ts`) — `GET /followup-settings` (200/500), `POST /followup-settings` (201/500, `zValidator` on `ZSaveFollowUpSettingsApiRequest`), both `checkAuth`-gated. Mounted in `workers/api/index.ts` as `app.route("/followup-settings", FollowUpSettingsRoutes)`, alongside the existing `/frameworks` line.
  - Verification: `tsc --noEmit` in `apps/backend` shows the identical error set as the Phase 2 checkpoint (no new errors from the 3 new files or the mount edit). `eslint` clean on all 4 touched/created files. Ran `wrangler dev` locally (port 8790) and curled both `GET`/`POST /followup-settings` without an auth token — both correctly returned 401 (not 404), confirming the route is mounted, `checkAuth`-gated, and `zValidator` wired. Could not exercise the authenticated save/fetch/version-increment round trip — no Clerk sign-in credentials available in this session (same limitation as prior units, e.g. Spec 07).
  - **Phase 1 (types only, no runtime wiring)**: new `packages/schemas/src/followup-settings/` (`FollowUpSettingsCommon.ts` — `ZFollowUpSettingsInput`/`ZFollowUpSettings`, `stepOffsetDays: number[]` capped `.max(12)`; `FollowUpSettingsApiRequest.ts`, `FollowUpSettingsApiResponse.ts`, `FollowUpSettingsDALRequest.ts`, `index.ts`), exported from `packages/schemas/src/index.ts`. `TasksCommon.ts`: added `TaskStatusIntEnum.Paused`/`TaskStatusLabelEnum.Paused` + label map entry, `stepNumber`/`pausedAt` added to `ZTaskRecord`. `TasksDALRequest.ts`: `SyncFollowUpDALRequest` now requires `stepNumber`; added `PauseFollowUpDALRequest`/`ResumeFollowUpDALRequest`/`DeleteFollowUpTasksDALRequest` (old `DeletePendingFollowUpDALRequest` kept alongside, marked `@deprecated`, until Phase 4's mechanical rename of the live `TasksDAL.deletePendingFollowUp` call site). `ContactsDALRequest.ts`/`ContactsApiResponse.ts`: added `GetSentMessageCountDALRequest`/`GetSentMessageCountApiResponse`. `log.ts`: added `GetSentMessageCount` (Contacts section), `PauseFollowUpTask`/`ResumeFollowUpTask`/`DeleteFollowUpTasks` (Tasks section, old `DeletePendingFollowUpTask` kept `@deprecated` until its one live call site in `TasksDAL.ts` is renamed in Phase 4), new `GetFollowUpSettings`/`SaveFollowUpSettings`.
  - **Phase 2 (DB migration)**: `tables.ts` — added `stepNumber`/`pausedAt` nullable columns to `tasks`; added new `followUpSettings` table (mirrors `jobSearchFrameworks` exactly — JSON `stepOffsetDays` text column default `"[]"`, `version`/`isCustomized`/`createdAt`/`updatedAt`, composite `(createdBy, version)` index). `Constants.ts` — added `FOLLOWUP_SETTINGS_DEFAULTS = { stepOffsetDays: [7] }` (preserves today's exact single-7-day behavior as the seeded default); `TASK_FOLLOWUP_INTERVAL_DAYS` deliberately left in place (its one call site in `ContactsRepo.resyncFollowUp` isn't rewritten until Phase 5). Migration `0014_isotope.sql` generated (`CREATE TABLE followup_settings`, `ALTER TABLE tasks ADD step_number/paused_at`) and applied to remote staging D1 via `pnpm db:migrate`.
  - Verification: `tsc --noEmit` clean standalone in `packages/schemas`. `apps/backend` has exactly one new compile error vs. baseline — `TasksRepo.ts:95` missing `stepNumber` on the `SyncFollowUpDALRequest` call — this is the plan's own explicitly-called-out expected checkpoint state (Phase 4 threads `stepNumber` through `TasksDAL`/`TasksRepo`). All other backend errors are pre-existing baseline errors (confirmed via `git stash` diff), unrelated to this change.
  - **Outstanding**: Phases 3-8 not started (Follow-up Settings backend, Tasks DAL/Repo primitives, `ContactsRepo` wiring, Follow-up Settings UI, Tasks UI updates, end-to-end verification).

- **Contact detail — company context (2026-07-21)**: Opening a contact (page route or slide-out panel) now shows the parent Company, all Jobs at that company, and all other Contacts at that company, alongside the contact's own info.
  - **`jobId` removed from Contact entirely** — contacts are only ever tied to a Company now, never a specific Job. Removed from `ZContactBase` (`packages/schemas/src/contacts/ContactsCommon.ts`), the `contacts` table (migration `0012_isotope.sql`, `ALTER TABLE contacts DROP COLUMN job_id` — **not yet applied to remote D1, `pnpm db:migrate` still needs to be run**), `ContactsDAL` (insert/selects/update, 6 spots), `ContactsRepo.updateContact`, and the bulk-edit "Job" column + `JobSelect` usage in `-DesktopContactsTable.tsx` (the only UI entry point that ever set it). `JobSelect.tsx` itself untouched — still used elsewhere for the Jobs entity.
  - **New endpoint**: `GET /companies/:id/jobs` (`CompaniesRoutes.ts`) → `JobsRepo.getJobsByCompany` → `JobsDAL.getJobsByCompany` (mirrors the existing `getContactsByCompany` pattern exactly — join on `companies`, filter `createdBy` + `companyId`, `.limit(20)` with a `// TODO: paginate` comment since pagination is deferred). New `GetJobsByCompanyDALRequest` type in `JobsDALRequest.ts`. `ContactsDAL.getContactsByCompany` also capped at `.limit(20)` with the same TODO (was previously unbounded).
  - **Shared detail component**: extracted `-ContactDetailContent.tsx` — the full contact header/tabs/body/footer, previously duplicated independently between the page route (`$contactId/index.tsx`) and the slide-out side panel (`-DesktopPanel.tsx`'s `ContactPanelContent`). Both surfaces now render this one component, differing only via props (`onClose`/`onExpand` for the panel, `onBack` for the page, shared `onDeleted`). Tab order changed from Draft/History/About to **History/About/Draft** in both surfaces (`ContactDetailTab` type + `TABS` array in the shared component).
  - **New `-CompanyContext.tsx`**: renders inside `AboutTab` (after Notes, before "Mark as dead") — Company summary card (status + fit-band badges, click-through to `/companies/$companyId`), Jobs list (click-through to `/jobs/$jobId`, empty state "No jobs tracked at this company yet"), Other Contacts list (excludes the current contact by id, click-through to `/contacts/$contactId`, empty state "No other contacts at this company yet"). Follows the existing `-LinkedContacts.tsx` list-row visual pattern.
  - **`-LinkedContacts.tsx` fix (in scope, same iteration)**: contact rows on the Company detail page previously had no click handler at all — now navigate to `/contacts/$contactId` on click (row changed from `div` to `button`).
  - **New query**: `JobsQueries.byCompany(companyId, getToken)` in `apps/web/src/routes/_authenticated/jobs/-data.ts`, hitting the new endpoint.
  - Verification: `tsc --noEmit` clean in `packages/schemas` and `apps/web` (zero errors). `apps/backend` has the same 21 pre-existing unrelated errors as the unmodified baseline (`EnvConfig.ts`, `dbClient.ts`, `AiProvider.ts`, `NotesRepo.ts`, `notes.test.ts`, `JobDiscoveryWorkflow.ts` — none touch files changed in this unit). `eslint` clean on every file touched or created in this unit; all remaining lint errors/warnings are pre-existing and in untouched files.
  - **Outstanding**: `pnpm db:migrate` (applies to remote **staging** D1, shared infra) has not been run — needs explicit user confirmation before executing, per the session's risk-of-shared-state rule.

- **spec 01 — Workers AI setup**: Added `"ai": { "binding": "AI" }` to `wrangler.jsonc` (base + staging + production envs). Ran `wrangler types` to regenerate `worker-configuration.d.ts` — `AI: Ai` now present in all env interfaces. Removed stale `@cloudflare/workers-types` entry from `tsconfig.json` (package not installed; superseded by generated runtime types). Created `apps/backend/src/providers/ai.ts` — `AiProvider` class with `run()` and `runWithRetry()` methods; `AI_MODELS` constants for Sonnet (general) and Haiku (personalisation research only).

- **Spec 003A — Jobs Database Schema**:
  - Schema: Added `jobs` table to `apps/backend/src/db/tables.ts`. 15 columns, 3 indexes (UNIQUE on `url`, IDX on `created_by`, IDX on `company_id`). `status` + `type` as IntEnum integers; `skills` as JSON text; `match_score` nullable real (v2.0 reserved).
  - Enums: Created `packages/schemas/src/jobs/JobsCommon.ts` — `JobStatusIntEnum` / `JobTypeLabelEnum` + label enums, int→label maps, `ZJobBase`, `ZJob`. Exported from `packages/schemas/src/index.ts`.

- **Spec 003B — Jobs Zod Schemas & System Contracts**:
  - `JobsApiRequest.ts`: `ZCreateJobApiRequest` (title + url required, url validated via `z.url()`; company_id, description, location, salary, source optional). `ZUpdateJobApiRequest` (all fields optional).
  - `JobsApiResponse.ts`: `CreateJobApiResponse`, `GetJobApiResponse`, `GetJobsApiResponse`, `UpdateJobApiResponse` — all extend `ApiResponse`.
  - `JobsDALRequest.ts`: `CreateJobDALRequest`, `FindJobDALRequest`, `GetJobsDALRequest`, `UpdateJobDALRequest`.
  - `jobs/index.ts`: exports all four modules.
  - `log.ts`: Added `CreateJob`, `GetJobDetails`, `ListJobs`, `UpdateJob`, `DeleteJob`, `RunJobIngestion`, `DuplicateJobBlocked` to `LogAction` enum.
  - Verification: `tsc --noEmit` clean; `ZCreateJobApiRequest.safeParse` without url fails Zod as expected.
  - Migration: Generated `src/db/migrations/0005_isotope.sql`, applied to remote D1 (`isotope-db`).
  - TypeScript: No new errors. Pre-existing errors in ContactsRepo/NotesRepo/notes.test unchanged.

- **Spec 003C — Jobs Backend Routes**:
  - `JobsDAL.ts`: `createJob`, `getJobDetails`, `getJobsList` — all tenant-scoped (`WHERE created_by = ?`), try/catch on every method, AppLogger on error + not-found paths, skills JSON parsed on read.
  - `JobsRepo.ts`: `createJob` (forces `type=Manual`, defaults `status=Applied`), `getJobDetails`, `getJobs` — AppLogger.info at start of each op; delegates to DAL.
  - `JobsRoutes.ts`: `POST /`, `GET /`, `GET /:id` — checkAuth first, zValidator on body/param, 201/200/404/500 status codes.
  - `index.ts`: Mounted at `/jobs` (linter normalised from `/v1/jobs`).
  - TypeScript: No new errors (pre-existing ContactsRepo/NotesRepo/notes.test errors unchanged).

- **Spec 003C — Jobs Status Enum Fix**:
  - Replaced 6-value status enum (Applied/Screening/Interview/Offer/Rejected/Withdrawn) with correct 8-value set: 1=NotStarted, 2=WaitingForHuman, 3=Accepted, 4=Applied, 5=CompanyAdded, 6=Interviewing, 7=Offer, 8=Rejected.
  - `NotStarted` is the default for Manual jobs (`type=1`); `WaitingForHuman` is the default for LLM jobs (`type=2`).
  - `jobStatusIntToLabel` map updated with explicit `Record<JobStatusIntEnum, JobStatusLabelEnum>` type.
  - `JobsRepo.createJob` default status changed from `Applied` → `NotStarted`.
  - No DDL migration needed (column type unchanged; dev/staging DB had no real rows).

- **spec 02 — Company Research Framework Form**:
  - DB: Added `frameworks` table to `apps/backend/src/db/tables.ts`. Generated migration `0004_isotope.sql` and applied to remote D1.
  - Schemas: Created `packages/schemas/src/frameworks/` — `FrameworksCommon.ts` (enums, `ScoredCriterion`, `CompanyFrameworkFormInputs`, `Framework`), `FrameworksApiRequest.ts`, `FrameworksApiResponse.ts`, `FrameworksDALRequest.ts`, `index.ts`. Added `FrameworkTypeIntEnum` (1=CompanyResearch). Exported from `packages/schemas/src/index.ts`.
  - Log: Added `GenerateFramework`, `SaveFramework`, `GetLatestFramework`, `GetFrameworkVersions` to `LogAction` enum.
  - Backend: `FrameworksDAL.ts` — `getLatestFramework`, `getFrameworkVersions`, `createFramework`. `FrameworksRepo.ts` — `generateCompanyFramework` (calls AiProvider with structured system prompt + user message), `saveCompanyFramework` (auto-increments version), `getLatestCompanyFramework`, `getCompanyFrameworkVersions`. `FrameworksRoutes.ts` — `POST /frameworks/generate`, `PUT /frameworks/company`, `GET /frameworks/company`, `GET /frameworks/company/versions`. Mounted in `index.ts`.
  - CSS: Added `--accent-bg` / `--accent-text` tokens to `apps/web/src/styles.css` (light/dark mode) as they were not yet defined.
  - Frontend shared: `apps/web/src/shared/fields/ScoredCriteriaField.tsx` — repeatable criterion builder with weight stepper, auto-no-go toggle, expand/collapse detail rows, drag-reorder on desktop and up/down arrows on mobile. `apps/web/src/shared/forms/CompanyResearchFrameworkForm.tsx` — full 6-section form (salary, locations tag-input, ethics tag-input, scored criteria, decision bands, auto no-go summary), validate-before-generate, live computed band example, "See an example" collapsible (onboarding only).
  - Frontend data: `apps/web/src/routes/_authenticated/onboarding/-data.ts` — `FrameworksQueries`, `useGenerateFramework`, `useSaveFramework` with explicit `onError` toast handlers.
  - Onboarding route: `apps/web/src/routes/_authenticated/onboarding/step-1.tsx` — wizard step 1 with form→review→navigate state machine; wizard header with progress dots; sticky footer with contextual actions.
  - Settings route: `apps/web/src/routes/_authenticated/settings/index.tsx` — Company Research Framework section with view (current framework + version history), edit (form), and review (editable textarea + save) steps. Relative timestamp helper.

- **Spec 003D — Jobs Frontend Data View**:
  - `-data.ts`: `JobsQueries` class with `list`, `count`, `detail` static methods + `useJobs`, `useJobsCount` hooks.
  - `-JobStatusBadge.tsx`: `JobStatusBadge` (8-status map) + `JobTypeBadge` (Manual/LLM with AI tokens).
  - `-JobsTable.tsx`: 8 columns (source/type defaultHidden), `AppTablePagination` rendered at bottom via `pagination` prop passed from parent.
  - `-JobDetailDrawer.tsx`: `JobDetailPanel` (desktop inline sliding aside, `w-100`) + `JobDetailMobileDrawer` (vaul bottom sheet `h-[90vh]`). Both controlled by `jobId: number | null`.
  - `AppTable`: Added `defaultHidden?: boolean` to `AppTableColumn`. Added `toolbarLeft?: ReactNode` prop.
  - `index.tsx`: Full route with `validateSearch` (`panel?: number`), client-side pagination (page size 20, sliced from server results), desktop flex-row layout, mobile list + mobile drawer.

- **Spec 003D+ — Jobs Search**:
  - Server-side search via SQLite `LIKE %term%` across `jobs.title`, `jobs.location`, `jobs.salary`, and `companies.name` (LEFT JOIN). Empty or absent `searchText` returns all jobs.
  - `LogAction.SearchJobs` added to `log.ts`.

- **Spec 003E — Manual Entry & Edit Form**:
  - Backend: `updateJob` added to `JobsDAL` (partial-field update via dynamic `setValues`, ownership check), `JobsRepo` (maps `UpdateJobApiRequest` → `UpdateJobDALRequest`), `PATCH /jobs/:id` route in `JobsRoutes.ts`.
  - `UpdateJobDALRequest` in `packages/schemas` updated to `Partial<NullableDALFields<...>>` so only `id`+`createdBy` are required — all update fields optional.
  - `-data.ts`: `useCreateJob` (`POST /jobs`) and `useUpdateJob` (`PATCH /jobs/:id`) mutation hooks with mandatory `onError` toast handlers. Sentry captured globally via `queryClient.ts` `mutations.onError`.
  - `-CompanySelect.tsx`: autocomplete dropdown — fetches all companies, filters by search input, supports clear, keyboard-accessible.
  - `-JobEntryForm.tsx`: TanStack Form dual-mode form. Create mode: all fields, required validation on `title` + `url`. Edit mode: pre-populated from `initialData`, same fields fully editable. Submits to correct mutation based on presence of `initialData.id`. Success: toast + `onSuccess()`. Required fields block submission with inline errors.
  - `index.tsx`: `formMode` state (`null | "create" | Job`). "Add job" button in both mobile and desktop headers. Edit button (pencil icon) in `JobDetailPanel`/`JobDetailMobileDrawer` header — passes job to `setFormMode`. Form rendered in modal overlay.
  - `-JobDetailDrawer.tsx`: `onEdit?: (job: Job) => void` prop added; pencil icon button shown when `onEdit` + loaded job both present.

- **Spec 003F — Job Search Framework**:
  - DB: Added `job_search_frameworks` table to `tables.ts` (15 columns, `idx_job_fw_user` index). Migration `0007_isotope.sql` generated and applied to remote D1.
  - Schemas: `packages/schemas/src/frameworks/` — `FrameworksCommon.ts` (`ZPrioritisedSkill`, `ZFramework`, `ZFrameworkInput`), `FrameworksApiRequest.ts`, `FrameworksApiResponse.ts`, `FrameworksDALRequest.ts` (`GetFrameworkDALRequest`, `CreateFrameworkDALRequest`, `SaveFrameworkDALRequest`), `index.ts`. Exported from `packages/schemas/src/index.ts` as `export * from "./frameworks"`. Old `job-search-frameworks/` directory deleted.
  - LogAction: `SaveFramework`, `GetFramework` in `packages/schemas/src/log.ts`.
  - Constants: `JOB_SEARCH_FRAMEWORK_DEFAULTS` in `apps/backend/src/config/Constants.ts` — single backend source of truth for default values.
  - Backend: `FrameworksDAL.ts` — `getFrameworkDetails`, `saveFramework` (fetches version, JSON-stringifies arrays, calls `createFramework`, sets `isCustomized: true`), `createFramework` (raw insert + prunes to 5 versions), `createDefaultIfAbsent` (idempotent seed from Constants). `FrameworksRepo.ts` — `seedDefault`, `getFrameworkDetails`, `saveFramework` (pure delegation to DAL — no DB logic). Routes: `GET /frameworks/job-search`, `POST /frameworks/job-search` in `FrameworksRoutes.ts`.
  - User seeding: `UsersRepo.syncClerkUser` calls `fwRepo.seedDefault(clerkId)` after successful upsert — every new user gets a default framework row with `isCustomized: false`.
  - Frontend data: `FrameworkQueries` (key: `["frameworks","job-search","latest"]`), `useSaveFramework` in `apps/web/src/routes/_without_nav/onboarding/job-search-framework/-data.ts`.
  - Frontend forms: `apps/web/src/shared/forms/JobSearchFrameworkForm.tsx` — reusable form using `FrameworkInput` type. Location section in right column (below Search Settings). No hardcoded defaults — always hydrated from DB row.
  - Onboarding route: `apps/web/src/routes/_without_nav/onboarding/job-search-framework/index.tsx` — skeleton while `isPending || !framework`; redirects to `/jobs` only when `framework.isCustomized === true`; passes DB row as `initialValues`.
  - Settings route: `apps/web/src/routes/_authenticated/settings/frameworks.tsx` — no `max-w` constraint; `showNotice = !noticeDismissed && framework != null && !framework.isCustomized`; skeleton while `isPending || !framework`; no hardcoded defaults.
  - Jobs page: `hasFramework` checks `frameworkQuery.data?.framework?.isCustomized` (not mere row presence); Discover button navigates to onboarding if `!hasFramework`.

- **Spec 003G — Job Discovery Workflow**:
  - `WebSearchProvider.ts`: `WebSearchProvider` interface + `TavilySearchProvider` — maps Tavily `/search` endpoint to `WebSearchResult[]`. `time_range` param for recency; throws on non-200.
  - `JobDiscoveryWorkflow.ts`: `JobDiscoveryWorkflow extends WorkflowEntrypoint` — 6 durable steps: fetch-framework → web-search (per targetRole query, URL-deduped) → extract-jobs (Workers AI JSON extraction) → filter-jobs (required skills hard-gate) → dedup-jobs (against existing `jobs` table) → insert-jobs (bulk insert, one-by-one with continue-on-error).
  - `JobsDAL.ts`: `getExistingUrls` (Set of existing URLs for dedup) + `bulkInsertJobs` (one-by-one, logs per-failure, returns insert count).
  - `JobsRepo.ts`: `discoverJobs` — checks framework, triggers `JOB_DISCOVERY_WORKFLOW.create()`, returns 400 if no customized framework.
  - `JobsRoutes.ts`: `POST /jobs/discover` — returns 202 immediately; 400 if no framework.
  - `wrangler.jsonc`: `workflows` binding `JOB_DISCOVERY_WORKFLOW` → `JobDiscoveryWorkflow` in base + staging + production envs. `TAVILY_API_KEY` placeholder var added.
  - `index.ts`: `export { JobDiscoveryWorkflow }` — required by Cloudflare Workflows runtime.
  - `wrangler types` re-run — `JOB_DISCOVERY_WORKFLOW: Workflow<...>` and `TAVILY_API_KEY: string` now in generated `Env`.
  - Frontend: `useDiscoverJobs` mutation in `-data.ts`. Both Discover buttons (desktop header + mobile banner) wired — disabled + "Searching…" while pending, success toast on 202, redirect to onboarding on 400, error toast on other failures.

- **Spec 04 — Contact History (CRUD)**:
  - LogAction: Added `GetContactHistory`, `CreateContactHistory`, `UpdateContactHistory`, `DeleteContactHistory` to `packages/schemas/src/log.ts`.
  - Schemas: Added `ContactHistoryDirectionEnum` (me/contact) and `ContactHistoryChannelEnum` (email/linkedin) to `ContactsCommon.ts`. Added `ZLogContactHistoryApiRequest` (direction+channel+body+sentAt), `ZUpdateContactHistoryApiRequest` to `ContactsApiRequest.ts`. Added `UpdateContactHistoryApiResponse`, `DeleteContactHistoryApiResponse` to `ContactsApiResponse.ts`. Added `FindContactHistoryDALRequest`, `UpdateContactHistoryDALRequest` to `ContactsDALRequest.ts`.
  - DAL: Updated `getContactHistory` (orderBy sentAt, correct LogAction). Updated `createContactHistory` (auto-increment sequencePosition for sent messages from prior max). Added `updateContactHistory` (body/sentAt/subject patch, ownership check). Added `deleteContactHistory` (ownership-scoped delete).
  - Repo: Added `logContactHistory` (derives `type` from direction+channel, e.g. `email_sent`). Added `updateContactHistory`, `deleteContactHistory`.
  - Routes: Changed `POST /:id/history` to use `ZLogContactHistoryApiRequest` via `logContactHistory`. Added `PATCH /:id/history/:historyId` and `DELETE /:id/history/:historyId`.
  - Frontend data: Updated `useCreateContactHistory` to use `LogContactHistoryApiRequest`. Added `useUpdateContactHistory`, `useDeleteContactHistory` mutation hooks.
  - Frontend UI: Replaced read-only `HistoryTab` with interactive version — `ComposeForm` (direction toggle, channel toggle, date picker, textarea), `EditHistoryForm` (inline edit per message), per-message edit/delete icon buttons, Touch N label on outgoing messages, reply count in header.

- **Spec 003H — Jobs Delete + Bulk Actions**:
  - Schemas: `ZDeleteJobApiRequest`, `ZBulkDeleteJobsApiRequest`, `ZBulkUpdateJobsApiRequest` added to `JobsApiRequest.ts`. `DeleteJobApiResponse`, `BulkDeleteJobsApiResponse`, `BulkUpdateJobsApiResponse` added to `JobsApiResponse.ts`. `DeleteJobDALRequest`, `BulkDeleteJobsDALRequest`, `BulkUpdateJobsDALRequest` added to `JobsDALRequest.ts`. `LogAction.BulkDeleteJobs`, `LogAction.BulkUpdateJobs` added to `log.ts`.
  - DAL: `deleteJob` (ownership-scoped DELETE, 404 if not found), `bulkDeleteJobs` (inArray DELETE, returns count), `bulkUpdateJobs` (inArray UPDATE status, returns count) added to `JobsDAL.ts`.
  - Repo: `deleteJob`, `bulkDeleteJobs`, `bulkUpdateJobs` delegation methods added to `JobsRepo.ts`.
  - Routes: `DELETE /jobs/:id` (404 on not-found), `DELETE /jobs/bulk` (body: `{ ids }` ), `PATCH /jobs/bulk` (body: `{ ids, status }`) registered in `JobsRoutes.ts`. Bulk routes registered before `/:id` to avoid param conflict.
  - Frontend data: `useDeleteJob`, `useBulkDeleteJobs`, `useBulkUpdateJobs` hooks added to `-data.ts`. `useDeleteJob.onSuccess` removes detail query cache entry. Bulk hooks invalidate `keys.all()`.
  - `JobPanelContent`: trash icon button in header; on delete → toast + calls `onDelete?.(id)`. `onDelete` prop threaded through `JobDetailPanel` / `JobDetailMobileDrawer`.
  - `JobsTable`: checkbox column added (select-one per row), select-all toggle in toolbar. Bulk action bar in toolbar replaces search when rows selected: status select + Apply, Delete, Clear.
  - `MobileJobsList`: "Select" toggle enters select mode; per-card checkbox; sticky bottom action bar with status select + Apply + Delete while in select mode.
  - `index.tsx`: `handleBulkDelete`, `handleBulkStatusUpdate`, `handlePanelDelete` wired; panel URL cleared on delete; all bulk mutations threaded into table + mobile list.

- **Spec 06 — Queue Setup**:
  - Created `isotope-queue` (producer binding `ISOTOPE_QUEUE`) and `isotope-queue-dlq` (DLQ — no consumer).
  - API worker entry point moved: `src/index.ts` → `workers/api/index.ts`. Wrangler config + .dev.vars files moved into `workers/api/`.
  - Processor worker entry point created at `workers/processor/index.ts` — `queue()` handler boilerplate; routes messages via `message.ack()` / `message.retry()`.
  - `workers/api/wrangler.jsonc`: added `queues.producers` binding for `isotope-queue` in staging + production envs.
  - `workers/processor/wrangler.jsonc`: consumer binding for `isotope-queue` with `dead_letter_queue: "isotope-queue-dlq"`, `max_retries: 3`, `max_batch_size: 10`, `max_batch_timeout: 30`.
  - `src/api/.dev.vars` + `.dev.vars.example`, `src/processor/.dev.vars` + `.dev.vars.example` created.
  - `package.json` scripts split into `dev:api`, `dev:processor`, `build:api`, `build:processor`, `deploy:api`, `deploy:processor`, `generate-types:api`, `generate-types:processor`.
  - CI: `deploy-worker-staging.yml` + `deploy-worker-production.yml` updated — workingDirectory → `apps/backend/src/api`, secrets path → `src/api/.dev.vars.example`.
  - CI: `deploy-worker-staging.yml` + `deploy-worker-production.yml` updated — each now deploys both API worker and processor worker in sequence within the same job.
  - **Manual steps required**: Run `wrangler queues create isotope-queue` and `wrangler queues create isotope-queue-dlq` in Cloudflare dashboard or CLI before deploying. Run `pnpm generate-types:api` and `pnpm generate-types:processor` (from `apps/backend/`) to regenerate `worker-configuration.d.ts` for each worker.

- **Browser Run redirect resolution + budget gate**:
  - `InboundJobAlertHandler.resolveRedirectUrl`: `HEAD` fetch with `redirect: "follow"` resolves email tracking URLs (e.g. `awstrack.me`) to final destination before scraping. 10s timeout, falls back to original on error. Resolved URL re-checked against blocklist. DB stores the final URL, not the tracker URL — fixes null title/description bug where Browser Run was scraping the tracker redirect page instead of the job page.
  - `BrowserRunBudgetDAL.ts`: `isShutdown()` reads single-row `browser_run_budget` table, auto-resets counter when `now >= reset_at` (lazy monthly reset — no cron needed), returns `true` when `used_seconds >= 28,800`. `recordUsage(elapsedSeconds)` atomically increments via SQL `used_seconds + ?`, inserts row on first call. Fails open on D1 error.
  - `browser_run_budget` table added to `tables.ts`. Migration `0008_isotope.sql` generated and applied to remote D1.
  - `packages/schemas/src/browserRunBudget/`: `BrowserRunBudgetCommon.ts` (types + `IncrementBrowserRunBudgetDALRequest`), `index.ts`. Exported from `packages/schemas/src/index.ts`.
  - `LogAction`: Added `BrowserRunBudgetChecked`, `BrowserRunBudgetShutdown`, `BrowserRunBudgetRecorded`, `BrowserRunBudgetReset`.
  - `Constants.ts`: Added `BROWSER_RUN_MONTHLY_BUDGET_SECONDS = 36_000`, `BROWSER_RUN_SHUTDOWN_THRESHOLD = 0.8`, `BROWSER_RUN_SHUTDOWN_SECONDS = 28_800`.
  - Gate in `InboundJobAlertHandler`: `isShutdown()` checked before each `scrapeJobUrl` call — skips scrape + logs warn if shutdown. Wall-clock elapsed time measured and passed to `recordUsage()` after each call.

- **Spec 05 — Email Job Ingestion**:
  - `LogAction`: Added `InboundEmailReceived`, `InboundEmailFetchFailed`, `InboundUrlExtracted`, `InboundScrapeStarted`, `InboundScrapeFailed`, `InboundJobInserted` to `packages/schemas/src/log.ts`.
  - `Constants.ts`: Added `BROWSER_RUN_URL`, `BROWSER_RUN_PROMPT`, `BROWSER_RUN_TIMEOUT_MS`. (Note: `INBOUND_ATS_DOMAINS` and `INBOUND_JOB_PATHS` were added then removed — see architecture decision below.)
  - `CompaniesDAL.ts`: Added `findCompanyByName` (case-insensitive `lower()` LIKE match, scoped to user).
  - `EmailInboundRoutes.ts`: `POST /api/email-inbound` — public, no auth. Verifies Resend svix signature via `resend.webhooks.verify()`. Returns 400 on bad sig, 200+ignore for non-`email.received` events. Extracts userId from `to[0]` local part, enqueues `{ type: "InboundJobAlert", action: "Process", emailId, userId }` to `ISOTOPE_QUEUE`, returns 200 immediately.
  - `SettingsRoutes.ts`: `GET /settings/inbound-address` — `checkAuth` required. Returns `{ isSuccess: true, address: "{userId}@{RESEND_INBOUND_DOMAIN}" }`.
  - `InboundJobAlertHandler.ts`: Queue consumer for `InboundJobAlert` messages. Fetches email HTML from Resend. Extracts all unique `https://` hrefs (no ATS domain filter — job alert emails wrap links in tracking redirects that defeat domain checks). Deduplicates against existing DB URLs. For each new URL: calls Cloudflare Browser Run `/json` (30s timeout) which follows redirects and scrapes the final landing page. If Browser Run returns null title, falls back to URL path slug. Resolves or creates stub company (`CompanyStatusIntEnum.WaitingHuman`). Inserts via `JobsDAL.createJob()` with `type=LLM, status=WaitingForHuman`. Acks every message.
  - `workers/api/index.ts`: Mounted `EmailInboundRoutes` at `/api/email-inbound` (public, before auth routes) and `SettingsRoutes` at `/settings`.
  - `workers/processor/index.ts`: Routes `InboundJobAlert` messages to `inboundJobAlertHandler`.
  - `workers/processor/wrangler.jsonc`: Added `d1_databases` binding for `isotope-db` in staging + production envs (processor needs DB).
  - `workers/processor/.dev.vars.example`: Added `RESEND_API_KEY`.
  - `apps/web/src/routes/_authenticated/settings/index.tsx`: Replaced "A/B Testing" tab with "Account" tab. Added `AccountTab` component — fetches `/settings/inbound-address`, displays read-only address field with copy-to-clipboard button (✓ icon feedback).
  - `-JobDetailBody.tsx` + `-JobPanelDetails.tsx`: Description section now always rendered. When `job.description` is null, shows warning banner (WarningCircleIcon + "Edit this job to paste the description manually").
  - **Manual steps required**: Run `pnpm generate-types:api` and `pnpm generate-types:processor` after deploying to regenerate `worker-configuration.d.ts` with new bindings. Set secrets via `wrangler secret put RESEND_WEBHOOK_SECRET`, `RESEND_INBOUND_DOMAIN`, `CLOUDFLARE_ACCOUNT_ID`, `CLOUDFLARE_API_TOKEN`, `RESEND_API_KEY` for both API and processor workers.

- **Spec 07 — Tasks / Follow-ups Feature**:
  - Schemas: `packages/schemas/src/tasks/` — `TasksCommon.ts` (`TaskStatusIntEnum` Pending=1/Completed=2/Missed=3, `TaskStatusLabelEnum`, `ZTask`/`ZTaskWithMeta` with joined `contactName`/`companyName`/`designation`/`overdueByDays`, `ZTaskCalendarDay`), `TasksApiRequest.ts`, `TasksApiResponse.ts`, `TasksDALRequest.ts`, `index.ts`. Exported from `packages/schemas/src/index.ts`. `LogAction` entries added: `GetTasksCalendar`, `GetTasksForDate`, `GetPastTasks`, `SearchTasks`, `UpdateTaskStatus`, `SyncFollowUpTask`, `SweepOverdueTasks`, plus `UpdateNextTouchDueAt` (Contacts).
  - DB: `tasks` table added to `apps/backend/src/db/tables.ts` (`id`, `createdBy`, `contactId` nullable FK, `title`, `dueAt`, `status`, `note`, `completedAt`, `createdAt`, `updatedAt` — all date/timestamp columns `t.text()` to match the 100% existing precedent across every other table, even though `code-standards.md` nominally says integer timestamps; flagging the discrepancy rather than silently introducing a one-off column type). Migration `0009_isotope.sql` generated and applied to remote D1.
  - Backend: `TasksDAL.ts` (`getTasksCalendar`, `getTasksForDate` — special-cases `date === today` to roll overdue Pending/Missed tasks forward, `getPastTasks`, `searchTasks`, `updateTaskStatus`, `syncFollowUpForContact` — upserts the contact's one open Pending task, `sweepOverdueTasks` — system-wide, no `created_by` filter). `TasksRepo.ts`, `TasksRoutes.ts` (`POST /tasks/calendar`, `POST /tasks/day`, `GET /tasks/past`, `POST /tasks/search`, `PATCH /tasks/:id`). Mounted at `/tasks` in `workers/api/index.ts`.
  - `ContactsDAL.updateNextTouchDueAt` added (partial single-column update — deliberately not reusing `ContactsDAL.updateContact`, which nulls out every unset field). `ContactsRepo.logContactHistory` now calls a new private `scheduleFollowUp` after any `*_sent` message: sets `contacts.nextTouchDueAt = sentAt + Constants.TASK_FOLLOWUP_INTERVAL_DAYS` (7) and calls `TasksRepo.syncFollowUpForContact`. This is the first code that ever actually writes `nextTouchDueAt` — previously dead schema.
  - Cron: `TaskMissedSweepHandler.ts` (new `handlers/` file) — sweeps overdue `Pending` → `Missed` daily. Wired via a new `scheduled()` export on the API worker (`workers/api/index.ts`, alongside existing `fetch()`) and `triggers.crons: ["5 0 * * *"]` added to `workers/api/wrangler.jsonc` (staging + production). First cron trigger in the codebase — `wrangler types` re-run for `ScheduledController` ambient type.
  - Frontend: `apps/web/src/routes/_authenticated/today/` renamed to `.../tasks/` (`git mv`) — replaced the 9-line placeholder. `-utils.ts` (pure date helpers, no date library — native `Date`, since `apps/web` has no `dayjs`/`date-fns` dependency and none was worth adding for this). `-data.ts` (`TasksQueries` + `useTasksCalendar`/`useTasksForDay`/`usePastTasks`/`useSearchTasks`/`useUpdateTaskStatus`, mandatory `onError` toasts). `-WeekStrip.tsx`, `-TaskRow.tsx`, `-TaskSection.tsx` (shadcn `Collapsible`). `index.tsx` — week strip + rolling 7-day sections (`useQueries`) + collapsed Past tasks section + search (replaces the day-sections view entirely when non-empty, per user's plan).
  - shadcn: added `checkbox.tsx`, `collapsible.tsx`, `badge.tsx` via `npx shadcn add` (`-c apps/web`) — all draw on the already-installed unified `radix-ui` package, no new npm dependency.
  - Nav: `HomeUtils.NAV_ITEMS` — `today`/`HouseIcon`/`/today` → `tasks`/`ClipboardTextIcon`/`/tasks`. Root `routes/index.tsx` redirect target updated to `/tasks`.
  - Contact deep link: `contacts/$contactId/index.tsx` gained `validateSearch` (`?tab=draft|history|about`), initial `activeTab` hydrated from it, tab clicks now also update the URL search param. `-TaskRow.tsx`'s "View conversation" link navigates to `/contacts/$contactId?tab=history`.
  - Verification: `tsc --noEmit` clean in all three packages (only pre-existing, unrelated `CompaniesCommon.ts`/`EnvConfig.ts`/`NotesRepo.ts`/etc. errors present before this change). `eslint` clean on every touched file. Ran both workers + web dev servers on non-default ports (8788/3001, to avoid the user's already-running 8787/3000 instances) — confirmed `/tasks/*` routes are mounted and `checkAuth`-gated (401 without token), and `/` → `/tasks` SSR redirect resolves the `_authenticated/tasks/` route successfully with no server errors. Could not verify the authenticated UI visually — no Clerk sign-in credentials available in this session.

- **Tasks feature code-review remediation (2026-07-05)** — 20 findings from a full staged-diff review fixed before commit:
  - **Day boundary unified**: backend "today" was worker-UTC `dayjs()` in 5 places while the frontend keyed dates in local time — broke the Today overdue rollup, `overdueByDays`, and sweep timing for non-UTC users. Now `Constants.APP_UTC_OFFSET_MINUTES` (330, IST) + `Utility.getDateKey()/getTodayDateKey()` is the single owner; all five call sites go through it. `ZDateKey` (YYYY-MM-DD regex) added to schemas and used by task request validators.
  - **Follow-up sync unified**: `ContactsRepo.resyncFollowUp(contactId)` recomputes from `getLastSentHistory` and runs after every history mutation — create (was using the just-logged `sentAt`, so backfilling an old message dragged the due date into the past), update (previously never resynced despite `sentAt` being editable), delete. It aborts when the read fails (`isSuccess` checked — previously a transient D1 error destructively cleared `nextTouchDueAt` and deleted the pending task), and writes the task first, `nextTouchDueAt` only on success, so the two representations can't diverge. `getContactDetails` full-row fetch dropped; `TasksDAL.syncFollowUpForContact` resolves the contact name itself (single narrow column) on the insert path.
  - **DAL contract**: `syncFollowUpForContact`/`deletePendingFollowUp`/`sweepOverdueTasks` returned `void`/`number` and swallowed errors — now all return `{isSuccess}` responses. Sweep uses `meta.changes` instead of materializing `.returning()` rows, and is invoked through `TasksRepo` (was handler → DAL, skipping a layer).
  - **Status labels moved to Repo**: `withMeta`/label mapping removed from `TasksDAL` (returns raw `TaskRecord` rows, new DAL response types in schemas); `TasksRepo.withMeta/withStatusLabel` is the one mapping site, per the documented NotesRepo pattern.
  - **Query fixes**: `searchTasks` escapes `%`/`_`/`\` with `LIKE … ESCAPE '\'` (raw input previously acted as wildcards); `getPastTasks` orders `dueAt DESC` (was ASC — >50 past tasks hid all recent ones) and the unused `count` query/field is gone; calendar no longer filters out `Missed` (new `hasMissed` on `TaskCalendarDay`, red dot in WeekStrip); composite index `IDX_tasks_created_by_due_at` replaces the single-column `created_by` index (migration `0010_isotope.sql` generated — **remote `db:migrate` not yet run**).
  - **History-type convention owned by schemas**: `buildContactHistoryType()`, `CONTACT_HISTORY_SENT_TYPES`, `_sent`/`_received` suffix constants in `ContactsCommon.ts` — replaces the `like(type, "%_sent")` pattern (where `_` was an unintended single-char wildcard), the repo's string interpolation, and HistoryTab's hardcoded literals. `getLastSentHistory` now returns typed `GetLastSentHistoryApiResponse`.
  - **History delete scoped to contact**: `deleteContactHistory` filters on `contactId` too and returns not-found when 0 rows change (previously `DELETE /contacts/999/history/42` deleted contact 7's row and resynced contact 999); history route params validated with `regex(/^\d+$/)`; PATCH history now passes `contactId` and triggers resync.
  - **Frontend**: Tasks page has error states for all four queries (none existed); day/search/past cards all render through one refactored `TaskSection` (collapsible flag, optional dateLabel — kills the triple copy-paste and the stray `·` separator); `updatingTaskId` derived from `mutation.isPending + variables` (redundant state + unhandled `mutateAsync` rejection removed); toggle mutation patches day/search caches via `setQueriesData` and background-invalidates past/calendar only (was awaited `invalidateQueries(all)`); duplicate `Content-Type` headers dropped (apiClient default); `-utils.ts` uses `toLocaleDateString` instead of hand-rolled month/weekday tables; unused `badge.tsx` deleted; contact detail `activeTab` derived from the URL search param (back/forward now switch tabs); query hooks follow the golden notes pattern (page-level `useAuth`, `TasksQueries.x(…, getToken)`).
  - **Cron logging**: `scheduled()` chains `disposeLogger()` after the sweep settles (was concurrent `waitUntil`s — dispose raced the sweep and could drop its logs).
  - **Dependency hygiene**: `wrangler` restored to `^4.0.0` (resolves 4.104.0; was an ephemeral pkg.pr.new PR-snapshot build) and `minimumReleaseAge: 10080` un-commented in `pnpm-workspace.yaml`.
  - Verification: `tsc --noEmit` and `eslint` clean on all touched files (pre-existing unrelated errors unchanged).

- **Mobile parity audit + Phase 0 foundation (2026-07-31)** — full desktop-vs-mobile audit found Jobs at full parity, Companies/Contacts drifting desktop-only on recent polish commits (bulk actions, copy-to-clipboard, bulk-add/bulk-log entry points missing on mobile; Contacts' mobile filter chips are decorative, no `onClick`), and Tasks/Settings with zero mobile investment — Settings had no nav entry point on mobile at all. (Notes was also flagged as "unreachable" by the audit, but Notes is an intentionally hidden/internal feature — LLM context only, not user-facing — so it is explicitly excluded from all mobile-parity work; see Architecture Decisions.) Root cause: no shared mobile-detection hook (3 separate ad-hoc `matchMedia` call sites) and `MobileTabBar`'s `NAV_ITEMS` never grew past its original 4 items. Phase 0 (foundation, this unit) shipped:
  - `useIsMobile()` hook (`apps/web/src/hooks/useIsMobile.ts`) — `md`/768px breakpoint, mirrors `useDebouncedValue`'s plain `useState`+`useEffect` style, no new dependency. Replaces the ad-hoc `matchMedia` in `jobs/-JobDetailDrawer.tsx` and `contacts/index.tsx`'s `useAddShortcut` viewport fork.
  - `HomeUtils.MORE_NAV_ITEMS` (`-utils.ts`) — Settings only, separate from the primary `NAV_ITEMS` (Tasks/Jobs/Companies/Contacts) since the mobile tab bar only has room for the primary set.
  - `MobileTabBar` is now `grid-cols-5`: 4 primary tabs + a "More" tab that opens a `vaul` `Drawer` bottom sheet (same primitive as `JobDetailMobileDrawer`) listing Settings and a Sign out action — mobile previously had **no sign-out control anywhere**, same `apiClient` revoke + Clerk `signOut()` flow as `DesktopSidebar.handleSignOut`, duplicated here since it's a small self-contained handler and the two components don't share a parent that could host it.
  - `context/ui-context.md` mobile shell spec line updated — was still describing the pre-rename "Today" nav and didn't mention the More sheet or `useIsMobile`.
  - Verification: `tsc --noEmit` and `eslint` clean on all touched files.
  - Remaining phases (not yet started): Phase 1 reachability polish (Settings mobile layout pass), Phase 2 Tasks mobile build (zero mobile code today — `-WeekStrip.tsx`'s `grid-cols-7 min-w-[64px]` and `index.tsx`'s fixed `w-64` search input both need mobile treatment), Phase 3 close Companies/Contacts bulk-action + copy + filter-chip + bulk-add/bulk-log gaps, Phase 4 process guardrail (any `-DesktopXTable.tsx` commit should touch the matching `-MobileX*.tsx` file too).
  - **Phase 1 skipped for now** — user directed work straight to Phase 2 then Phase 3. Settings still has no dedicated mobile layout pass (renders the same desktop form layout on narrow screens); revisit if it proves unusable on mobile in practice.

- **Mobile parity Phase 2 — Tasks mobile build (2026-07-31)** — Tasks had zero mobile code before this unit (confirmed via audit: no `-Mobile*.tsx` files, no `md:` classes anywhere in the folder). Shipped as a `hidden md:flex` / sibling `md:hidden` split, same pattern as Jobs:
  - `-MobileWeekStrip.tsx` — compact 7-col grid strip, no fixed per-cell min-width (desktop's `-WeekStrip.tsx` uses `min-w-[64px]` per cell = 448px floor, doesn't fit 375px viewports); dropped the "Today" jump button to save width since tapping today's cell does the same thing.
  - `-MobileTaskRow.tsx` — stacked title/meta/tags layout instead of desktop `-TaskRow.tsx`'s row of up to 5 separate badges (channel/step/paused/overdue/date), which doesn't fit a narrow row; kept the "View conversation" deep link to contact history but dropped `target="_blank"` (new-tab intent is desktop-only UX, mobile browsers handle it inconsistently).
  - `-MobileTaskSection.tsx` / `-MobileTasksHeader.tsx` / `-MobileTasksList.tsx` — mirror desktop `-TaskSection.tsx`'s collapsible logic and `index.tsx`'s day/search/past section wiring; header replaces the fixed `w-64` search input (was overflowing next to the page title on narrow screens) with a Jobs-style icon-toggle search field.
  - No bulk-action work needed here — Tasks has no bulk endpoints (checked `-data.ts`: only a single `useUpdateTaskStatus` mutation), unlike Companies/Contacts/Jobs.
  - Verification: `tsc --noEmit` and `eslint` clean. Not visually verified in a browser — `chromium-cli` (the project's browser-driving tool per the `run` skill) is not installed in this environment, and the `/tasks` route requires an authenticated Clerk session with no credentials available in this session (same blocker noted in the original Tasks feature's verification history above).

- **Mobile parity Phase 3 — Companies/Contacts bulk actions, copy, filters (2026-07-31)** — closed the drift the audit found between desktop and mobile on these two features:
  - **Companies**: `-MobileCompanyRow.tsx` gained `selectMode`/`selected`/`onToggleSelect` props (checkbox row instead of `Link` when active, mirroring `-MobileJobsList.tsx`'s row pattern). `-MobileCompaniesList.tsx` gained a "Select" header toggle and a bottom bulk-action bar with delete only — Companies has no bulk-update mutation (`useBulkDeleteCompanies` is the only bulk hook in `-data.ts`), so no bulk-edit control was added; matches desktop `-DesktopCompaniesTable.tsx`'s own bulk toolbar, which is also delete-only.
  - **Contacts** (the larger gap): `-MobileContactRow.tsx` gained copy-to-clipboard buttons for email/LinkedIn (was completely absent on mobile — commit `e9ec772` added this to `-DesktopContactsTable.tsx` only) and the same `selectMode` pattern as Companies. `-MobileContactsList.tsx`'s filter chips were previously decorative (`active: true` hardcoded on "All", no `onClick` on any chip, no data behind "Replied"/"Re-engage"/"Dead") — replaced with real filtering against `ContactStatusIntEnum` (`packages/schemas/src/contacts/ContactsCommon.ts`), matching desktop's status values exactly. Added a bottom bulk-action bar (status update via the shared `STATUS_OPTIONS` from `-AddOrEditContactModal.tsx`, plus delete) since Contacts, unlike Companies, does have `useBulkUpdateContacts`. Added a bulk-log entry point (icon button linking to `/contacts/bulk-log`, was desktop-only) and an Add-contact FAB (mobile previously had no visible add affordance at all).
  - Verification: `tsc --noEmit` and `eslint` clean on all touched files (one pre-existing unrelated lint error in `companies/$companyId/-PreFiltersCard.tsx`, not touched by this change).

- **Mobile parity follow-up — bulk-add contacts on mobile (2026-07-31)** — user flagged that bulk-add (`/contacts/bulk-add`) was still unreachable on mobile after Phase 3: the Phase 0 `useAddShortcut` fork and the Phase 3 FAB both routed mobile to the single-add `AddOrEditContactModal`, while desktop's "Add" button always went to the newer bulk-add page (per commit `5d3f08a`'s original deliberate note, "mobile keeps the existing modal"). The bulk-add route itself was the real blocker — a fixed 9-column `<table>` (`table-fixed`, percentage-width `colgroup`) with no responsive treatment at all, unusable under ~700px.
  - `-MobileBulkAddCard.tsx` — new stacked-card row (name/company/title/email/LinkedIn/status fields, skip/delete actions) reusing the same shared pieces as desktop's `-BulkAddRow.tsx` (`CompanySelect`, `STATUS_OPTIONS`, `TitleRolePills`, `DuplicateContactWarning` — all already render fine at modal width, confirmed since `-AddOrEditContactModal.tsx` already uses them on mobile). Initial cut simplified the name-autofill guard to "only fill if the name field is empty" — **corrected below, this was a real bug, not an acceptable narrowing.**
  - `bulk-add.tsx` split into `hidden md:block` (table) / `md:hidden` (card list) — same pattern as every other feature, so this route is no longer a single shared desktop-only layout. The bottom summary/action bar also now stacks vertically on mobile instead of a fixed horizontal row.
  - User confirmed mobile's Add entry points (FAB in `-MobileContactsList.tsx`, Alt+T shortcut in `index.tsx`) should now navigate straight to `/contacts/bulk-add`, matching desktop exactly, rather than keep the old single-add modal as a fallback. The `useIsMobile()` viewport fork added in Phase 0 for this exact shortcut is now removed — both platforms take the identical code path. `mode="add"` usage of `AddOrEditContactModal` in `contacts/index.tsx` (and its `showAddModal` state) was dead after the switch and was deleted; `mode="edit"` usage in `-ContactDetailContent.tsx` is untouched and still needed.
  - Verification: `tsc --noEmit` and `eslint` clean on all touched files.

- **Mobile parity bug fixes from code review (2026-07-31)** — three real defects caught reviewing the Phase 3 / bulk-add mobile work, all fixed same-day:
  - **`-MobileContactRow.tsx` — invalid HTML / a11y**: the default (non-select) row wrapped its content in `<Link>` (→ `<a>`), but that content includes the email/LinkedIn copy `<button>`s added in Phase 3 — `<button>` nested inside `<a>` is invalid HTML; browsers reparent the button out of the anchor at parse time, so the live DOM silently diverges from the React tree and screen readers see broken/duplicate interactive elements. `e.preventDefault()`/`stopPropagation()` on the button's own click handler doesn't fix a structural nesting defect. Fixed by changing the default branch from `<Link>` to the same `<div role="link" tabIndex={0} onClick={navigate(...)}>` pattern the `selectMode` branch already used safely (a plain `<div>`, not an anchor, so nested buttons are valid).
  - **`-MobileBulkAddCard.tsx` — autofill regression**: the "only fill if empty" guard (see above) stops autofilling permanently after the _first_ guess, because `onChange({ name: guess })` makes the field non-empty on that very first call — every subsequent keystroke in email/LinkedIn then sees a non-empty name and bails, even though the user never touched the name field themselves. Desktop's `-BulkAddRow.tsx` instead tracks the last value _it_ wrote in a `lastAutofilledName` ref and only stops once the field diverges from that — letting the guess keep refining as the user keeps typing. Ported the identical ref-guard logic into the mobile card so behavior matches exactly.
  - **`-MobileCompaniesList.tsx` / `-DesktopCompaniesTable.tsx` — fire-and-forget bulk delete**: `handleBulkDelete` in both components called `onBulkDelete(ids)` without awaiting and cleared the selection unconditionally, so a failed delete silently lost the user's selection with no way to retry — unlike Contacts' `handleBulkDelete` (both desktop and mobile), which awaits, only clears on success, and keeps the selection on failure so the error toast (already fired by the mutation's `onError`) has something to retry. Traced to the root: `companies/index.tsx` passed `(ids) => void handleBulkDelete(ids)` into both table components as a synchronous `(ids: number[]) => void` prop, discarding the real `async` `handleBulkDelete`'s promise before either component could ever await it — a pre-existing defect in the desktop table (Companies' original golden pattern for this Phase 3 unit) that got copied into the new mobile file rather than caught. Fixed at the source: `onBulkDelete` prop type changed to `(ids: number[]) => Promise<unknown>` on both `-DesktopCompaniesTable.tsx` and `-MobileCompaniesList.tsx`, `companies/index.tsx` now passes `handleBulkDelete` directly (no `void` wrapper), and both components' `handleBulkDelete` are now `async`/await/catch, matching Contacts' pattern exactly.
  - **`-MobileWeekStrip.tsx` — missing "Today" jump control**: the Phase 2 build dropped desktop `-WeekStrip.tsx`'s "Today" button to save horizontal space, on the assumption that tapping today's cell in the grid was equivalent — false whenever today isn't in the currently-displayed week (i.e. after using the prev/next-week arrows), since today's cell simply isn't rendered in an out-of-range week and desktop's `onToday` does two things a cell-tap can't: `setWeekAnchor(new Date())` (snap the visible week back to the current one) as well as `setSelectedDateKey(todayKey)`. There was no way back to today after paging away from it on mobile. Fixed by adding a `CalendarDotIcon` icon-button next to the prev/next arrows, wired to the same `onToday` closure desktop's `WeekStrip` already uses (`setWeekAnchor(new Date()); setSelectedDateKey(todayKey);`), threaded through `-MobileTasksList.tsx` and `index.tsx`.
  - **`-MobileCompanyRow.tsx` / `-MobileContactRow.tsx` — keyboard-inoperable select rows, plus a checkbox double-toggle**: the `role="button"` (select mode) and `role="link"` (default, Contacts only) row wrappers had `tabIndex={0}` and `onClick` but no `onKeyDown` — a plain `<div>` with an ARIA role doesn't get the browser's native Enter/Space activation a real `<button>`/`<a>` has for free, so focusing a row with keyboard and pressing Enter or Space did nothing (WCAG 2.1.1 violation). Fixed by adding `onKeyDown` to both row branches in both files (Enter+Space for `role="button"`, Enter-only for `role="link"`, matching native element semantics). Separately, in select mode the inner checkbox `<button>`'s `onClick` called `preventDefault()` but not `stopPropagation()`, so a mouse click bubbled up to the row's own `onClick` — both fired, toggling selection on then back off, so tapping the checkbox specifically never actually selected/deselected anything (only tapping elsewhere in the row worked). Fixed by adding `stopPropagation()` to the checkbox handler in both files.
  - Verification: `tsc --noEmit` and `eslint` clean on all touched files.

- **Mobile parity simplifications from code review (2026-07-31)**:
  - **Deduplicated `-MobileCompanyRow.tsx` / `-MobileContactRow.tsx`**: the checkbox button, trailing chevron svg, row wrapper className, and the keyboard-operable `role`-aware shell (with the `onKeyDown`/`stopPropagation` fixes above) were near-byte-identical across both files — the exact kind of duplication that let the same three bugs above need fixing twice. Extracted into `_authenticated/-MobileListRow.tsx` (`MobileListRowCheckbox`, `MobileListRowChevron`, `MobileListRowShell`, `MOBILE_LIST_ROW_CLASS`), placed at the `_authenticated/` root alongside `-DesktopSidebar.tsx`/`-MobileTabBar.tsx` since it's now shared across the `companies/` and `contacts/` route folders, not local to either. Both row files shrank (Contacts: 138 → 106 lines) with no behavior change.
  - **Removed `-MobileTasksList.tsx`**: it was a pure pass-through wrapper between `TasksPage` and its own mobile leaves (`MobileTasksHeader`/`MobileWeekStrip`/`MobileTaskSection`) — 28 props re-declared in its own `Props` interface and forwarded one level deeper, adding no logic of its own. Every other mobile split in this codebase (Jobs — the golden pattern — plus Companies and Contacts) renders its mobile list as a direct sibling inside the route component itself, no intermediate wrapper; Tasks' version only grew a wrapper because its mobile view has more sub-sections (header + week strip + day/search/past sections) than the flatter list+header shape the other features have. Inlined the wrapper's JSX directly into `tasks/index.tsx`'s `md:hidden` block, matching the established pattern, and deleted the file. No prop-drilling layer left — `TasksPage`'s local state/query variables are used directly by the mobile leaves, same as they already were for the desktop leaves in the same component.
  - Verification: `tsc --noEmit` and `eslint` clean on all touched files.

- **Multi-select status filters + saved filters across Jobs/Companies/Contacts (2026-08-04)**:
  - **Server-side filtering for all three tables.** Added `statuses` (and `fitBands` for Companies) to `ZGetJobsApiRequest` / `ZGetContactsApiRequest` / `ZGetCompaniesApiRequest` and their DAL request types, applied via `inArray` in each DAL. A shared `ZIntFilterList` codec in `common.ts` normalises the three wire shapes (JSON array, `?statuses=1,2`, repeated `?statuses=1&statuses=2`) to `number[]`. Client-side filtering was not an option: Contacts paginates server-side, so filtering the current 20 rows would have produced wrong counts and page ranges.
  - **`JobsDAL.buildJobsFilter`** — `getJobs` and `getJobsCount` previously duplicated their WHERE clause; the filter is now built once and shared so a filtered page and its total can never diverge.
  - **Jobs moved to true server-side pagination.** `jobs/index.tsx` fetched all rows and sliced client-side while `JobsRoutes` silently discarded `pageNo`/`pageSize` (the DAL already supported `limit`/`offset`). With server-side filters this mismatch would have made `useJobsCount` return filtered totals against an unfiltered slice. Routes now forward all paging params, and the page requests only its current page.
  - **Query keys now carry every server-side parameter** (filters + paging) in all three `-data.ts` files. Previously `["jobs","list",searchText]` etc. would have served one filter selection's cached rows to another.
  - **New `saved_filters` feature** (migration `0025_isotope.sql`) — full stack per CLAUDE.md: `packages/schemas/src/saved-filters/` (5 files + index), `SavedFiltersDAL/Repo/Routes`, mounted at `/saved-filters`. `criteria` is stored as a JSON string so new filter dimensions need no migration; `SavedFiltersDAL.parseCriteria` degrades a corrupt row to empty criteria rather than failing the whole list. Unique index on `(created_by, entity_type, name)`.
  - **DAL vs API response split** — `Get/Create/UpdateSavedFilterDALResponse` return raw `SavedFilter`; the Repo's `withLabels` attaches `savedFilterEntityType`/`savedFilterEntityTypeLabel` for the API-level types. Keeps the "DAL returns raw int, Repo maps the label" rule intact.
  - **Shared UI at `_authenticated/` root** (alongside `-MobileListRow.tsx`): `-StatusFilterPopover.tsx` (multi-select via Popover + Command + Checkbox), `-SavedFilterBar.tsx` (filter chips, save/rename/delete, dirty-state "Update filter"), `-table-filters.ts` (URL param codec + dirty comparison), `-saved-filters-data.ts`. One `filterBar` node is built per page and passed to **both** the desktop table and the mobile list, so the two can't drift.
  - **Active filters live in URL search params** (`?statuses=1,2&fitBands=1&savedFilter=3`), matching the existing `panel` param — shareable and refresh-safe.
  - **Removed the three redundant client-side mobile filters**, per user instruction to keep mobile identical to web: Jobs' `STATUS_TABS` + `MOBILE_GROUPS`, Contacts' `applyMobileFilter` chips + draft/pipeline/other grouping, and Companies' chips + needs-review/in-progress grouping. All three mobile lists are now flat, server-filtered lists. Deleted `companies/-CompaniesFilterBar.tsx` (its single-select status + fit filters are now multi-select in the shared bar) and its `applyFilters` helper.
  - Verification: `tsc --noEmit` clean on `apps/web` (0 errors); `apps/backend` unchanged against its 76-error pre-existing baseline; `eslint` clean on all touched files.
  - **Migrations applied.** `wrangler.jsonc` has no separate local/dev environment — `dev:api` runs `wrangler dev --env staging` against the real staging D1 (`remote: true`), so staging is the dev database in this project's topology, not a shared-only target. Ran `pnpm db:migrate` (`--remote --env staging`); confirmed both `status_change_notes` (0024) and `saved_filters` (0025) tables exist on staging. CLAUDE.md's `db:migrate:local` reference was corrected — that script never existed and there is nothing for it to target.

## In Progress

- Mobile parity catch-up: Phases 0, 2, and 3 complete. Phase 1 (Settings mobile layout) and Phase 4 (process guardrail doc) remain.
- Tasks page "View conversation" now opens the contact detail side panel (`?panel=<contactId>`) on desktop instead of a new browser tab, reusing `contacts/-DesktopPanel.tsx`. Mobile rows still navigate to `/contacts/$contactId`, matching the mobile contacts list.

## Next Up

- Phase 1: Settings mobile page layout pass (currently reuses the desktop form layout as-is).
- Phase 4: document the "touch both Desktop and Mobile files in the same commit" guardrail in `code-standards.md`.
- Visual verification of Phases 2 and 3 in an authenticated browser session — blocked in this environment on both Clerk credentials and a missing `chromium-cli` install.

## Open Questions

- The original Today Dashboard's other 6 sections (Needs your input, Needs attention, Drafts ready, Stalled drafts, Companies to review, Jobs to review) were never implemented and are not part of the Tasks page — decide whether/where they should live (a separate page, or folded into Tasks) as a future unit.
- ~~Received messages do not cancel/complete the follow-up task~~ — **resolved by the Follow-Up Sequences feature**: an inbound reply now pauses the active follow-up task (`TasksRepo.pauseFollowUpForContact`) rather than leaving it Pending indefinitely; the next outbound message advances the sequence and naturally supersedes the paused row.
- ~~`apps/backend`'s vitest/`cloudflare:test` harness is broken~~ — **fixed (2026-07-21)**: see "Vitest/Workers test harness fix" below.

## Architecture Decisions

- **Notes is a hidden/internal feature, never user-facing nav** — it exists as LLM context/golden-rules storage for the app, not a feature end users browse. Never add it to `HomeUtils.NAV_ITEMS`, `MORE_NAV_ITEMS`, `-DesktopSidebar.tsx`, `-MobileTabBar.tsx`, or any other nav surface. Confirmed by user (2026-07-31) after it was mistakenly added to both navs during mobile-parity Phase 0.
- Workers AI binding named `AI` — consistent with Cloudflare convention and the `Ai` type in generated runtime types.
- `AiProvider` wraps `env.AI` in a class (matching `ClerkProvider` pattern already in codebase) — instantiated per request inside route handlers.
- `AI_MODELS.haiku` reserved exclusively for contact personalisation research per `code-standards.md`; all other AI work uses `AI_MODELS.sonnet`.
- `@cloudflare/workers-types` removed from `tsconfig.json` — `wrangler types` generates equivalent runtime types and the package was not installed.
- `frameworks` table uses integer `type` column (`FrameworkTypeIntEnum`) and auto-incremented `version` per user per type — latest is always `ORDER BY version DESC LIMIT 1`.
- Onboarding step-1 navigates to `/today` on framework save (step-2 route does not exist yet — will be updated when spec 03 is implemented).
- `--accent-bg` / `--accent-text` CSS tokens added to `styles.css` Block 4 — were referenced in ui-context.md but not yet defined.
- `Constants.DEFAULT_COMPANY_RESEARCH_FRAMEWORK` — canonical Appendix A framework document stored as a markdown string constant in `apps/backend/src/config/Constants.ts`. Injected into the `generateCompanyFramework` system prompt as a reference template.
- **Query route convention** — read-only endpoints that need a request body use `POST /jobs/query` sub-paths within the same resource router. All routes for a resource stay in one `<Feature>Routes.ts` file. `Constants.DEFAULT_PAGE_NO` and `Constants.DEFAULT_PAGE_SIZE` applied in Repo so routes and DAL stay decoupled from default business logic.

- **Browser Run rate limiting storage choice** — KV rejected (eventual consistency → race condition: two concurrent queue batches both read stale count, both proceed past threshold). Durable Objects rejected (overengineered for ≤3,600 calls/month volume; idle GB-second cost). D1 chosen: serialized SQLite writes eliminate race conditions; 2 D1 ops/call costs $0 against the 75M free ops/month; ~5ms overhead is negligible against a 10s scrape call. Lazy monthly reset (checked on every `isShutdown()` read) eliminates need for a cron job.

- **Browser Run redirect resolution** — Tracker/redirect URLs (e.g. `awstrack.me`, `click4.instahyre.com`) stored as source URL in DB caused Browser Run to scrape the redirect interstitial instead of the job page, resulting in null title/description. Fix: `HEAD` fetch with `redirect: "follow"` resolves to final URL before scraping and before storing in DB.

- **Tasks table date columns as text, not integer timestamps** — `code-standards.md` says timestamps should be `t.integer({ mode: "timestamp" })`, but every single existing table (`users`, `companies`, `contacts`, `contact_history`, `jobs`, `notes`) uses `t.text()` + `Utility.getCurrentISOTimestamp()`. Followed the 100% real precedent over the written-but-unfollowed rule rather than introduce the first inconsistent table.
- **Follow-up task auto-creation lives in `ContactsRepo`, not a new cron reading `contact_history`** — the moment a sent message is logged is the natural trigger (matches the "7 days later (no reply)" language in `project-overview.md`); a cron scanning for "was a message sent 7 days ago with no matching newer row" would be equivalent but slower to reflect and harder to reason about. `TasksRepo.syncFollowUpForContact` is called directly from `ContactsRepo`, not via an HTTP round-trip.
- **Missed-task cron is the first `scheduled()` handler in the codebase** — added directly to the existing API worker (`workers/api/index.ts`) rather than a new dedicated worker, since it's a lightweight daily sweep with no queue/workflow needs of its own.
- **Inbound email URL extraction strategy** — ATS domain + job-path filtering was dropped. Job alert emails (e.g. Instahyre) wrap every link in a tracking redirect domain (e.g. `click4.instahyre.com`) so the final ATS hostname is never visible pre-click. The new approach extracts all unique `https://` hrefs and passes them directly to Browser Run, which follows redirects natively and scrapes the final page. Non-job pages return null/partial data which is still stored (title falls back to URL path slug). This is simpler and handles all email providers without needing a domain allowlist.

## Session Notes

- Pre-existing TypeScript errors in `ContactsRepo.ts`, `NotesRepo.ts`, `notes.test.ts`, and contacts panel files are unrelated to this unit and were present before this change.
