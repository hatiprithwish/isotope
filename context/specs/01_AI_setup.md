Read CLAUDE.md very carefully before implementing anything.

- Use /cloudflare:cloudflare skill to understand how to configure workers AI in a repo.
- Configure Workers AI full setup with best practices - create the AI module under `workers/providers` folder which will be used throughout the app in future.
