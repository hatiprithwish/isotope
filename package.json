{
  "name": "isotope",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:worker": "pnpm --filter backend dev",
    "dev:web": "pnpm --filter web dev",
    "build:worker": "pnpm --filter backend build",
    "build:web": "pnpm --filter web build",
    "generate-types": "pnpm --filter web generate-types && pnpm --filter backend generate-types",
    "lint": "pnpm -r lint",
    "lint:fix": "pnpm -r lint:fix",
    "format": "prettier . --write",
    "format:check": "prettier . --check",
    "prepare": "husky"
  },
  "lint-staged": {
    "**/*.{ts,tsx,js,jsx,json,md}": "prettier --write"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.4.0",
    "eslint-config-prettier": "10.1.8",
    "globals": "^17.6.0",
    "husky": "^9.1.7",
    "lint-staged": "17.0.5",
    "prettier": "3.8.3",
    "typescript": "^ 6.0.3",
    "typescript-eslint": "^8.59.3"
  }
}
